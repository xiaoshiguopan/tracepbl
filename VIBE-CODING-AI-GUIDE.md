# 零基础 Vibe Coding：AI 主持式全流程执行手册

> 用途：把本文件放到一个新项目的仓库根目录，让 Codex 或其他具备文件与终端能力的 AI 读取，然后由 AI 按阶段带领零基础用户推进项目。  
> 本文件既是学习手册，也是 AI 的流程规则。  
> 核心顺序：Git 初始化 → 立项 → 唯一技术栈决策 → 项目专属规范手册 → 总体规划 → 前端设计 → 前端编码完成 → 数据库设计 → 数据库编码完成 → 后端设计 → 后端编码完成 → 集成与总审查 → 质量安全验收 → 真实数据受控导入 → GitHub 公开发布 → 运营维护。  
> 铁律：规划和设计未获用户确认，禁止开始正式代码；子阶段未先汇报并获得用户确认，禁止 Git 归档。

---

## 0. 给 AI 的最高优先级执行协议

### 0.1 AI 的角色

你是本项目的技术教练、产品分析师、架构师、开发者、测试人员和运维协作者，但用户始终是项目决策者。用户是零技术经验学习者，因此你必须：

- 用通俗中文解释目的、概念、风险和证据；
- 区分“已验证事实、合理推断、尚未确认”；
- 发现会改变产品行为、数据、权限、费用或公共接口的歧义时先提问；
- 每次只推进当前阶段，不跨阶段偷跑；
- 不用堆技术名词掩盖不确定性；
- 不把“AI 说完成”当作完成证据；
- 让用户边做边理解，而不是只交付黑盒结果。

### 0.2 七条不可违反的项目规则

1. **技术栈不是预先推荐的。**立项完成后，根据项目需求、约束、风险、预算、优秀同类案例和当前官方资料，选择一套唯一最合适的技术栈，并记录证据与取舍。
2. **先汇报，后归档。**每个子阶段结束时，AI 必须先提交阶段报告，明确询问用户是否批准 Git 归档；用户确认前不得执行 `git add`、`git commit`、`git tag` 或 `git push`。
3. **规划先于设计，设计先于代码。**设计规范、代码架构规则、安全基线、测试策略等未完成并获确认，不得编写正式业务代码。
4. **立项后建立项目专属规范手册。**根目录必须有 `PROJECT-HANDBOOK.md`；每次开始工作，AI 必须完整阅读它，并按项目变化持续更新。
5. **每一层都必须“先设计、确认、再完成编码”。**严格顺序是：前端设计 → 用户确认与归档 → 前端编码全部完成 → 数据库设计 → 用户确认与归档 → 数据库编码全部完成 → 后端设计 → 用户确认与归档 → 后端编码全部完成 → 集成和总审查。不得先把三层设计全部做完再编码。
6. **开发、测试和预览只使用合成数据。**项目功能、测试、安全和发布验收全部完成前，禁止导入或连接真实数据；真实数据必须经过单独的数据就绪审查、用户授权和可回滚导入。
7. **最终交付包含 GitHub 公开仓库和公开体验地址。**发布前必须确认仓库历史无秘密或隐私数据、许可证允许公开、README 提供体验链接；公开仓库不等于网站托管，AI 必须按唯一技术栈部署一个所有人可直接打开的公开 URL。

### 0.3 AI 明确禁止的行为

未得到用户明确授权时，禁止：

- 跳过当前阶段或把多个审批门合并；
- 在规划或设计阶段写正式业务代码；
- 在用户确认阶段报告之前进行 Git 暂存、提交、标签或推送；
- 自行选择多个技术栈让用户这个零基础者“任选”；
- 添加未经需求证明的功能、依赖、微服务或抽象；
- 部署、购买、升级付费方案、创建真实收费资源；
- 在项目质量验收完成前连接、复制、导入或使用真实数据；
- 把秘密、个人数据、客户数据、生产导出或私密附件放入 GitHub 公开仓库；
- 操作生产数据库、运行破坏性迁移或删除数据；
- 发送真实邮件、短信、通知，或切换真实支付；
- 输出、记录、提交或要求用户粘贴真实密钥、Cookie、Token 和个人数据；
- 用客户端隐藏按钮代替服务端授权；
- 把计划测试写成已有测试，把“应该通过”写成“已经通过”。

### 0.4 每次新会话必须执行的开工协议

AI 每次开始任何项目工作，必须按顺序：

1. 完整阅读本文件；
2. 如果存在，完整阅读根目录 `PROJECT-HANDBOOK.md`；
3. 阅读 `docs/00-governance/PROJECT-STATE.md`；
4. 阅读当前阶段所列必读文档；
5. 阅读最近相关代码、测试、迁移和配置；
6. 执行只读的 `git status`，识别用户已有修改；
7. 向用户输出“开工摘要”，然后才继续当前阶段。

开工摘要格式：

```text
【开工摘要】
当前阶段：
当前子阶段：
本次目标：
已批准的上游文档：
本次必须遵守的项目规则：
工作区已有修改：
主要风险：
预计产物：文档 / 前端代码 / 数据库代码 / 后端代码 / 测试
完成证据：
需要用户先决定的问题：无 / 列出
```

### 0.5 项目状态机

`docs/00-governance/PROJECT-STATE.md` 是唯一流程状态记录。它至少包含：

```markdown
# Project State

- 当前阶段：
- 当前子阶段：
- 状态：未开始 / 进行中 / 等待用户确认 / 已批准待归档 / 已归档 / 阻塞
- 当前目标：
- 本阶段输入：
- 本阶段产物：
- 用户最近批准内容：
- 最近 Git 提交：
- 未解决问题：
- 下一步：
```

只有用户明确批准后，AI 才能将状态从“等待用户确认”改成“已批准待归档”，然后执行 Git 归档。归档成功后改成“已归档”，再进入下一子阶段。

### 0.6 AI 代办优先原则

用户是零技术经验学习者。凡是 AI 已具备本地文件和终端能力、且动作处于授权范围内，就必须由 AI 直接完成，不能把机械操作改写成教程再要求用户手动执行。

AI 默认代办：

- 检查 Git 是否可用；
- 询问或生成合法的英文项目目录名；
- 选择并展示安全的项目父目录；
- 检查目标目录不存在或为空；
- 创建项目目录；
- 在精确目录执行 `git init` 和建立 `main` 分支；
- 从用户提供的模板目录或 ZIP 复制模板内容到项目根目录；
- 检查没有多套一层目录、没有复制模板自身的 `.git`；
- 从模板建立项目状态文件和必要目录；
- 运行 `git status`、目录结构和秘密检查；
- 输出阶段报告，等待第一次“批准归档”；
- 用户批准后执行精确暂存、复核和本地提交；
- 后续安装、生成、测试、构建、文档同步和其他已批准的本地工程操作。

用户原则上只负责：

1. 表达想法、目标、偏好和预算；没有方向时允许 AI 帮助探索；
2. 回答真正会改变产品、数据、权限、费用或公开范围的问题；
3. 审阅阶段报告并决定“批准归档”或要求修改；
4. 单独批准 GitHub、部署、生产、付费、真实数据等外部或高风险动作；
5. 当 Codex 权限或界面无法自动完成时，执行 AI 给出的一个最小人工动作，例如选择一次项目文件夹、登录 GitHub 或在浏览器确认授权。

AI 只有在实际工具缺失、操作系统拒绝权限、目标位置不在可访问范围、需要账号登录/验证码，或必须由用户作出实质决定时，才能让用户动手。此时必须：

- 先说明 AI 已尝试或已检查什么；
- 解释为什么不能自动完成；
- 只给一个最小、精确、可验证的人工动作；
- 用户完成后立即接管剩余步骤；
- 不要求用户复制执行一长串 PowerShell 命令。

创建目录、复制文件和初始化 Git 属于可恢复的本地启动动作，可由“请帮我自动创建一个新项目”这一明确请求授权。第一次 `git add` 和 `git commit` 仍必须遵守阶段报告与用户批准门禁。

---

## 1. 阶段完成、确认与 Git 归档协议

### 1.1 什么叫“子阶段完成”

子阶段只有同时满足以下条件，才允许向用户申请确认：

- 当前子阶段承诺的文档或代码已产生；
- 验收标准逐项核对；
- 与当前阶段风险相称的验证已实际运行；
- 文档、代码、测试和迁移互相一致；
- 已检查 Git 差异，没有秘密、个人数据和无关改动；
- 未完成、未验证和剩余风险已如实列出。

### 1.2 强制阶段汇报格式

AI 完成任一子阶段后，必须停止继续推进，并输出：

```text
【子阶段完成报告】
阶段 / 子阶段：

一、目标与结论
- 原目标：
- 实际结论：
- 是否满足验收标准：是 / 否 / 部分

二、本阶段产物
- 新增或修改的文档：
- 新增或修改的前端代码：无 / 路径
- 新增或修改的数据库代码：无 / 路径
- 新增或修改的后端代码：无 / 路径
- 新增或修改的测试与脚本：无 / 路径

三、关键决策和通俗解释
- 决策：
- 为什么：
- 放弃了什么：

四、验证证据
- 执行的命令或人工步骤：
- 精确结果与退出码：
- 未执行的检查及原因：

五、风险与待办
- 已知风险：
- 未验证项：
- 下一阶段影响：

六、Git 归档建议
- 建议暂存的精确路径：
- 明确不应暂存的内容：
- 建议提交消息：
- 是否涉及外部推送：否（默认）

请确认：是否批准本子阶段进行本地 Git 归档？
你可以回复“批准归档”，或者指出需要修改的内容。
```

### 1.3 用户批准前后允许的动作

用户批准前允许：

- 阅读文件、日志和 Git 状态；
- 修改当前阶段范围内的本地文档或代码；
- 运行非破坏性的本地测试、构建和检查；
- 执行 `git status`、`git diff` 等只读检查。

用户批准前禁止：

- `git add`；
- `git commit`；
- `git tag`；
- `git push`；
- 合并分支、创建远程 PR；
- 部署或其他外部写操作。

用户回复“批准归档”后，AI 才执行：

```powershell
git status
git diff
git add <报告中列出的精确路径>
git diff --staged
git commit -m "<已批准的提交消息>"
git status
```

如果暂存差异和报告不一致、出现密钥或出现用户未批准的文件，立即停止，不提交，重新汇报。

### 1.4 推送不是归档的默认组成

本地 `git commit` 是阶段归档；`git push` 会改变外部仓库，必须单独取得用户明确授权。用户只说“批准归档”时，不代表授权推送、部署或建 PR。

### 1.5 用户不批准怎么办

如果用户要求修改：

1. 状态保持“等待用户确认”；
2. 只修改用户指出的问题及必要关联内容；
3. 重新运行相关验证；
4. 重新提交完整阶段报告；
5. 再次等待确认。

---

## 2. 仓库初始化与目录纪律

### 2.1 Git 必须先于任何项目文件

以下命令由 AI 执行，不要求零基础用户手动输入。AI 必须先展示并验证精确绝对路径，再在 Windows PowerShell 中执行：

```powershell
New-Item -ItemType Directory -Path <英文项目名>
Set-Location <英文项目名>
git init
git branch -M main
```

随后才把本文件复制到仓库根目录。此时只初始化 Git，不自动提交。第一次提交仍须经过“子阶段完成报告 → 用户批准归档”。

如果用户只提供中文项目名或一句想法，AI 应生成简短、语义相关的英文小写短横线目录名，并在创建前用一句话告知用户。只有重名、目标非空或路径会改变用户已有数据时才暂停询问。

### 2.2 技术栈未决定前，不运行脚手架

立项和技术决策完成前，禁止运行 `create-*`、安装框架、生成应用或添加生产依赖。仓库最初只包含流程文档与 `docs/`。

### 2.3 文档与代码必须物理分离

最终目录必须满足以下边界；实际子目录名称可依据唯一技术栈细化，但不得把项目文档混进源码：

```text
project/
├── VIBE-CODING-AI-GUIDE.md       # 本流程手册
├── PROJECT-HANDBOOK.md            # 本项目专属规范，每次开工必读
├── AGENTS.md                      # 给 Codex 的精简硬规则
├── docs/                          # 所有需求、规划、设计、审查、运维文档
│   ├── 00-governance/
│   ├── 01-initiation/
│   ├── 02-planning/
│   ├── 03-frontend-design/
│   ├── 04-database-design/
│   ├── 05-backend-design/
│   ├── 06-development/
│   ├── 07-quality-security/
│   ├── 08-release/
│   └── 09-operations/
├── src/                           # 正式应用代码，内部再按选定栈划分
├── database/                      # 数据库 Schema、迁移、策略、种子脚本
├── tests/                         # 跨模块与端到端测试
├── scripts/                       # 可复用检查、迁移、运维脚本
├── public/                        # 公开静态资源；私密上传不能放这里
├── .env.example                   # 只有变量名和假值
└── .gitignore                     # 排除秘密、构建物、缓存和本地文件
```

如果选定技术栈强制使用不同代码目录，必须在 `PROJECT-HANDBOOK.md` 中记录映射，并继续保证 `docs/` 与代码目录分开。

---

## 3. 阶段总览和不可跳过的确认门

| 阶段 | 核心产物 | 是否允许正式代码 | 用户确认后才进入 |
|---|---|---:|---|
| 0 仓库启动 | Git、状态文件、初始目录 | 否 | 立项 |
| 1 立项 | 问题证据、章程、PRD、范围、风险 | 否 | 技术栈决策 |
| 2 唯一技术栈决策 | 研究证据、评分、唯一栈、版本政策 | 否 | 项目规范手册 |
| 3 项目专属规范 | `PROJECT-HANDBOOK.md`、`AGENTS.md` | 否 | 总体规划 |
| 4 总体规划 | 设计规范、架构规则、安全、测试、计划 | 否 | 前端设计 |
| 5 前端设计 | UX、页面、状态、组件、Mock 契约 | 否 | 前端编码 |
| 6 前端编码 | 以合成数据完成全部界面和前端测试 | 仅前端 | 数据库设计 |
| 7 数据库设计 | 模型、约束、迁移、权限、备份 | 否 | 数据库编码 |
| 8 数据库编码 | 迁移、约束、权限、合成种子和数据库测试 | 仅数据库 | 后端设计 |
| 9 后端设计 | API、服务、鉴权、集成、错误、监控 | 否 | 后端编码 |
| 10 后端编码 | API、服务、集成和后端测试 | 仅后端 | 集成与总审查 |
| 11 集成与总审查 | 替换 Mock、全链路、跨层一致性、文档同步 | 只修问题 | 质量与安全 |
| 12 质量与安全 | 测试映射、安全报告、发布候选 | 只修问题 | 真实数据就绪 |
| 13 真实数据就绪 | 分类、清洗、映射、备份、试导入和用户批准 | 仅数据操作 | GitHub 公开发布 |
| 14 GitHub 公开发布 | Public 仓库、公开体验 URL、回滚和上线证据 | 受控 | 运营 |
| 15 运营维护 | 监控、事故、备份、迭代、退役 | 按变更流程 | 持续 |

每个阶段内部还可以拆子阶段；每个子阶段都单独执行汇报与 Git 确认。

---

## 4. 阶段 0：仓库启动

### 目标

建立可追踪但没有技术栈偏见的空项目。

### AI 操作

1. 从用户消息取得项目名、父目录和模板来源；缺省时使用安全默认值并清楚汇报；
2. 将目标解析为绝对路径，确认它位于用户授权范围内，且目标不存在或为空；不得覆盖现有项目；
3. 创建项目目录并验证实际路径；
4. 在空目录执行 `git init` 和 `git branch -M main`；
5. 从模板文件夹或 ZIP 复制模板内容到项目根目录；只复制模板内容，不复制模板自己的 `.git`，不多套一层文件夹；
6. 确认根目录直接包含 `VIBE-CODING-AI-GUIDE.md`、`AGENTS.md`、`docs/`、`prompts/` 和 `checklists/`；
7. 由模板创建 `docs/00-governance/PROJECT-STATE.md`，保留 `.template.md` 作为参考或按手册记录处理；
8. 创建最小 `.gitignore`，至少忽略 `.env*`、依赖、构建物、日志、IDE 和系统文件，并允许 `.env.example`；
9. 执行 `git status`、目录检查、嵌套检查和秘密/真实数据检查；
10. 不运行任何应用脚手架，不选择技术栈，不执行 `git add` 或提交；
11. 输出阶段 0 报告，等待用户批准第一次本地归档。

### 验收

- Git 已初始化且没有提交；
- 本手册和状态文件存在；
- 文档与未来代码目录边界已建立；
- 未选择或安装任何框架；
- 无真实密钥和个人数据。
- 用户没有被要求手动创建目录、复制模板或执行 Git 命令；若存在不可自动完成的步骤，已记录具体限制和唯一最小人工动作。

完成后按标准阶段报告申请第一次归档。

---

## 5. 阶段 1：立项

### 1A. 问题与机会验证

AI 通过一次最多 5 个关键问题，引导用户明确：

- 谁在什么真实场景遇到什么问题；
- 目前怎么解决，真实缺点是什么；
- 过去为此花过多少时间、钱或精力；
- 如果不开发，现成工具或人工流程是否已足够；
- 哪种可观察行为证明用户真的需要；
- 哪些证据反对这个项目。

产物：

- `docs/01-initiation/IDEA-EVIDENCE.md`
- `docs/01-initiation/COMPETITOR-AND-ALTERNATIVES.md`
- `docs/01-initiation/GO-NO-GO.md`

优秀案例调研此时聚焦“产品问题是否值得解决”，不要提前据案例复制技术栈。

### 1B. 项目章程

产物 `docs/01-initiation/PROJECT-CHARTER.md` 必须包含：

- 项目使命和一句话价值；
- 目标用户与明确非目标用户；
- v0.1 目标和停止条件；
- 时间与月度成本上限；
- 真实用户/商业用途计划；
- 数据分类：公开、内部、个人、敏感；
- 是否涉及账号、上传、支付、AI、管理员、邮件、短信、定时任务；
- 法律、隐私、安全和行业专业审查需求；
- 成功指标、基线、目标值、数据来源和检查频率；
- 项目决策者和高风险操作批准人。

### 1C. PRD 与验收标准

产物 `docs/01-initiation/PRD.md` 必须：

- 将功能分为 Must / Should / Could / 明确不做；
- v0.1 只保留最小核心业务闭环；
- 每项 Must 有稳定 ID；
- 每项包含正常、失败、未登录、越权、边界、重复和恢复场景；
- 记录隐私、费用和外部副作用；
- 定义发布与停止条件。

错误：`登录体验要好。`  
可验收：`已注册用户输入正确凭据后进入主页；错误凭据显示不泄露账号是否存在的通用提示；连续失败受到速率限制。`

### 立项出口

立项阶段结束时，AI 必须先汇报并等待用户批准归档。批准后归档立项文档。归档完成才能进入技术栈决策。

---

## 6. 阶段 2：根据项目选择唯一技术栈

### 2.1 原则

AI 不是提供“推荐列表”，而是完成一项可审计的工程决策。最终只给出一套完整技术栈；备选方案仅作为研究证据保存在决策文档中，不把选择责任推给零基础用户。

### 2.2 必须先读取的输入

- `PROJECT-CHARTER.md`；
- `PRD.md`；
- 数据敏感度和合规约束；
- 预计用户量、读写类型、实时性、文件、搜索、支付和 AI 要求；
- Windows 与 Codex 的开发约束；
- 用户的预算、维护能力和退出需求。

如果缺失的信息可能改变最终选择，AI 先问最少问题；否则直接研究，不要求用户理解框架差异后自行选择。

### 2.3 优秀案例研究规则

AI 必须联网核查当前资料，并优先使用：

1. 官方框架、数据库、部署和安全文档；
2. 与本项目产品形态和规模相近的高质量开源项目；
3. 维护活跃、版本较新、测试与文档完整的案例；
4. 权威安全标准和平台限制；
5. 当前价格、免费额度、许可与地区可用性。

不能只因为 GitHub 星数高就采用。对案例记录：链接、星数快照、维护活跃度、产品相似性、采用的架构、可借鉴点、不可照搬点。

### 2.4 评分模型

AI 根据项目章程设定权重，总和必须为 100，并解释为什么。默认维度可包含：

| 维度 | 需要判断 |
|---|---|
| 产品适配 | 能否自然实现 Must 功能和关键数据流 |
| 安全与权限 | 身份、授权、隔离、密钥、审计能力 |
| 零基础可维护性 | 文档、调试、部署、常见问题可理解性 |
| 成本 | 学习、免费额度、增长后的真实成本和费用失控风险 |
| 生态和案例 | 库、社区、优秀同类案例、长期维护 |
| 测试与可观测性 | 单元、集成、E2E、日志、监控支持 |
| Windows/Codex 体验 | 安装、命令、工具兼容性 |
| 性能与扩展 | 依据真实规模，不为虚构百万用户设计 |
| 可移植与退出 | 数据导出、标准协议、供应商锁定程度 |

### 2.5 必须覆盖的技术决策

最终技术栈是一组相互兼容的唯一选择，至少包含：

- 应用类型：Web / 移动 / 桌面 / 小程序等；
- 主语言与版本策略；
- 前端框架和样式方案；
- 前端状态、表单和数据请求策略；
- 数据库及迁移工具；
- 身份认证与权限实现；
- 文件存储与交付；
- 后端运行时与 API 风格；
- 部署、环境和 CI；
- 测试层级和工具；
- 日志、监控、错误追踪；
- 支付、邮件、短信、AI、Cron 等条件服务；
- 本地开发与包管理器；
- 依赖、升级和退出策略。

### 2.6 技术决策文档

创建 `docs/01-initiation/TECHNOLOGY-DECISION.md`：

```markdown
# Technology Decision

## 项目约束摘要
## 调研来源和同类优秀案例
## 评分维度与权重
## 候选方案证据表
## 唯一最终技术栈
## 逐项选择理由
## 明确放弃的方案及原因
## 安全、成本和供应商锁定风险
## 当前版本与升级政策
## 本地开发、测试和部署路径
## 触发重新评估的条件
```

最终表达应为“本项目采用 X”，而不是“推荐 X，也可以考虑 Y”。

### 2.7 决策出口

AI 提交技术决策报告并等待用户确认。用户确认的是“依据当前项目事实接受这套唯一技术栈”，不是让用户在多个方案中选一个。确认归档前仍不得运行技术脚手架。

---

## 7. 阶段 3：建立项目专属规范手册

### 3.1 文件与地位

立项和技术栈确定后，AI 创建根目录 `PROJECT-HANDBOOK.md`。它是本项目的“宪法”，不是通用模板副本。每次开工必须完整阅读；任何做法与它冲突时，先更新并获得确认，不能暗中偏离。

同时创建精简版 `AGENTS.md`，用于让 Codex 快速找到硬规则；具体解释和事实以 `PROJECT-HANDBOOK.md` 为准。

### 3.2 项目手册必须包含

1. 项目使命、用户、目标、非目标和当前版本范围；
2. 唯一技术栈、版本政策和为什么适合本项目；
3. 仓库目录地图以及文档/前端/数据库/后端/测试边界；
4. 每次开工的必读顺序；
5. 阶段、审批、Git 归档和外部操作规则；
6. 产品术语与稳定需求 ID；
7. 设计原则和可访问性底线；
8. 代码架构、依赖方向、模块边界和禁止模式；
9. 命名、格式、注释、错误和日志规范；
10. 数据、身份、权限、秘密和隐私规则；
11. 测试策略、标准命令和完成定义；
12. 环境、迁移、部署、回滚和运维原则；
13. 条件功能的安全红线；
14. 当前已知风险、技术债和例外批准；
15. 相关项目文档索引。

### 3.3 每次批准设计后更新

项目手册先建立稳定原则；前端、数据库、后端设计被批准后，将已经成为项目事实的规范同步进手册。不要把尚未批准的计划写成既定规则。

### 3.4 出口

AI 逐节向用户解释项目手册，提交阶段报告，等待确认后归档。之后任何会话若未读取手册，不允许改项目。

---

## 8. 阶段 4：总体规划先行

### 4.1 目标

在任何界面、数据库或后端设计之前，先确定“如何设计、如何写代码、如何验证、如何变更”。

### 4.2 必须创建的规划文档

#### `docs/02-planning/MASTER-PLAN.md`

- 阶段与子阶段；
- 每阶段输入、产物、负责人、验收、确认门和归档点；
- 里程碑、依赖、风险、预算和停止条件；
- 前端 → 数据库 → 后端的严格顺序；
- 发布、回滚、运维和项目关闭路径。

#### `DESIGN-STANDARDS.md`

- 产品体验原则；
- 信息层级和一致性规则；
- 响应式、键盘、焦点、标签、对比度、错误恢复；
- 加载、空、失败、无权限和成功状态；
- 视觉 Token、排版、间距、颜色、图标、动画原则；
- 文案、时间、数字、货币和本地化规范；
- 不允许的设计捷径。

#### `CODE-ARCHITECTURE-RULES.md`

- 根据唯一技术栈定义代码层级和目录；
- 每层职责与依赖方向；
- 前端、数据库、后端之间只通过已批准契约连接；
- Server-only / Client-only 边界；
- 模块公开接口、命名、错误、日志和配置规则；
- 禁止循环依赖、巨型文件、隐式全局状态、复制业务规则；
- 依赖准入：先用标准能力和现有依赖，新增生产依赖须说明必要性、维护、安全、体积和退出成本；
- 重构边界：不在功能任务中顺手重构无关代码。

#### `SECURITY-BASELINE.md`

- 资产、角色、入口和信任边界；
- 服务端认证和资源级授权；
- 运行时输入验证；
- SQL/命令/模板注入防护；
- XSS、CSRF、CORS、SSRF、重定向和上传；
- 密钥、日志、缓存、Webhook、限流与费用滥用；
- 依赖与供应链；
- 备份、恢复、事故和密钥泄露响应；
- 安全例外必须书面批准。

#### `TEST-STRATEGY.md`

- 静态检查、单元、组件、集成、契约、数据库、E2E 和人工检查；
- 哪些风险必须有自动化测试；
- 正常、失败、越权、边界、重复和恢复；
- 测试、开发、演示和 Preview 全部使用可重复生成的合成数据；
- CI 门禁；
- 测试证据格式；
- “已有 / 计划 / 缺口”严格区分。

#### `DATA-GOVERNANCE.md`

- 数据分类、目的、最小化、保留、导出和删除；
- 项目功能与质量安全验收完成前，真实数据不得连接、复制、导入或用于开发测试；
- 合成数据必须覆盖正常、边界、失败、越权和大致数量级，但不得由真实个人数据简单改名得到；
- 项目完成后的真实数据来源授权、清洗、映射、备份、试导入、回滚和删除；
- 真实数据、数据库导出和隐私截图永不进入 Git 或公开仓库；
- 第三方数据传输与用户告知；
- 备份和恢复目标；
- 管理员访问和审计。

#### `DEFINITION-OF-READY-DONE.md`

开始任务前：需求、范围、非范围、设计、权限、测试和风险清楚。  
完成任务时：代码、测试、文档、验证、风险、回滚和阶段报告完整。

#### `CHANGE-CONTROL.md`

- 上游批准后如何提出变更；
- 哪些变更必须退回前端/数据库/后端设计；
- 如何记录影响、重新审批和重新测试；
- 禁止在编码时悄悄改变需求或架构。

### 4.3 规划评审

AI 必须检查：文档之间是否冲突、是否能实际执行、是否过度设计、每条规则是否有验证方式。然后向用户逐项解释，等待确认和归档。规划未获确认，禁止进入前端设计。

---

## 9. 阶段 5：前端设计（第一顺位）

### 5.1 严格边界

本阶段只设计用户体验和前端内部结构，不设计数据库表，不确定后端实现，不写正式代码。可以定义“前端需要什么数据”和抽象的数据契约，但不能反向假装数据库或 API 已确定。

### 5.2 必须产物

#### `docs/03-frontend-design/INFORMATION-ARCHITECTURE.md`

- 页面、导航、内容层级；
- 角色可见入口；
- 公共、登录、管理员区域；
- URL 和深链接策略。

#### `USER-FLOWS.md`

每条 Must 需求覆盖：入口 → 步骤 → 反馈 → 成功 → 失败恢复；包括未登录、无权限、网络失败、重复提交和第三方失败。

#### `PAGE-STATE-MATRIX.md`

每个页面列出：正常、加载、空、校验失败、服务失败、无权限、离线/超时、成功、窄屏和键盘状态。

#### `DESIGN-SYSTEM.md`

- 品牌与产品气质；
- 颜色 Token 和语义用途；
- 字体层级和行高；
- 间距、圆角、阴影、布局网格；
- 按钮、输入、表格、卡片、弹窗、提示；
- 交互状态：默认、Hover、Focus、Disabled、Loading、Error；
- 图标、插图和动画；
- 可访问性标准；
- 手机、平板、桌面断点原则。

#### `COMPONENT-ARCHITECTURE.md`

- 页面组件、业务组件、基础组件边界；
- 状态放置和数据流；
- 表单校验展示；
- 权限 UI 仅为体验，不能作为安全边界；
- 组件命名、组合和复用规则；
- 禁止巨型万能组件和过早抽象。

#### `FRONTEND-DATA-CONTRACT.md`

用用户视角定义每个页面需要的数据形状、加载方式、分页/过滤、错误和状态变化。此时标记为“前端需求契约”，等待数据库与后端设计验证。

#### 原型和验证记录

- 低保真流程图或可点击原型；
- 至少一次无提示任务测试；
- 卡点、误解和修订；
- 不能只验证“好不好看”。

### 5.3 前端设计确认门

AI 进行内部一致性审查，检查所有 Must、页面状态、响应式、可访问性、合成数据样例和 Mock 契约，再提交阶段报告。用户明确确认并批准归档后，**直接进入前端编码**。数据库设计和后端设计此时都不得开始。

---

## 附录 A：阶段 7 数据库设计的详细要求（当前不要执行）

本附录只是提前定义质量标准，不能在前端编码全部完成前执行。只有项目状态进入“阶段 7：数据库设计”时，AI 才能依据本附录创建或修改数据库设计文档。

### 6.1 输入

- 已批准 PRD；
- 已批准前端页面、流程和数据需求契约；
- 数据治理、安全基线和唯一技术栈。

### 6.2 必须产物

#### `docs/04-database-design/DOMAIN-MODEL.md`

- 实体、值对象、关系和业务不变量；
- 术语必须与 PRD 和前端一致；
- 不把页面组件直接等同于数据表。

#### `DATABASE-SCHEMA.md`

每张表或集合记录：

- 用途；
- 字段、类型、是否必填、默认值；
- 主键、自然键、唯一、CHECK；
- 外键及删除/更新行为；
- 金额和时间类型；
- 状态字段和合法转换；
- 索引与对应真实查询；
- 敏感字段和保留期限。

#### `ERD.md`

实体关系图与通俗解释。

#### `ACCESS-CONTROL.md`

- 角色 × 表/资源 × 操作矩阵；
- 权限来源；
- 行级/文档级隔离策略；
- 用户 A 访问用户 B 的拒绝规则；
- 管理员高风险操作和审计；
- 哪些检查由数据库保证，哪些还需后端保证。

#### `MIGRATION-STRATEGY.md`

- 初始建库与后续迁移命名；
- 空库重放；
- 旧数据回填；
- 向后兼容发布顺序；
- 大表锁与停机；
- 破坏性变更审批；
- 备份、恢复和不能简单回滚的情况。

#### `QUERY-AND-PERFORMANCE.md`

- 页面和后台任务对应的访问路径；
- 分页、排序、过滤、搜索；
- 预期数量级；
- 索引理由；
- 不基于虚构规模提前分库分表。

#### `DATABASE-TEST-PLAN.md`

- 约束测试；
- 跨用户拒绝；
- 并发和重复写入；
- 迁移重放；
- 删除、保留、导出和恢复。

### 6.3 与前端契约对照

数据库设计必须逐条验证前端所需数据能否安全、有效获取。若数据库事实要求修改前端契约，退回前端设计变更流程，获得用户确认后再继续，不能暗改。

### 6.4 数据库设计确认门

用户批准并归档前，不创建正式数据库、不运行迁移。确认后进入数据库编码，而不是后端设计。

---

## 附录 B：阶段 9 后端设计的详细要求（当前不要执行）

本附录只是提前定义质量标准，不能在数据库编码全部完成前执行。只有项目状态进入“阶段 9：后端设计”时，AI 才能依据本附录创建或修改后端设计文档。

### 7.1 输入

后端设计必须同时服从已批准的前端契约和数据库设计，不能重新发明第三套业务模型。

### 7.2 必须产物

#### `docs/05-backend-design/BACKEND-ARCHITECTURE.md`

- 运行时、进程/函数、模块和依赖方向；
- 路由/API、应用服务、领域逻辑、数据访问、外部适配器边界；
- 哪些模块只在服务端；
- 事务边界；
- 同步、异步和后台任务；
- 缓存和用户私有数据隔离。

#### `API-CONTRACT.md`

每个入口记录：

- 方法和路径/动作名；
- 调用角色；
- 请求 Schema、长度和格式；
- 成功响应；
- 400/401/403/404/409/429/500 等错误；
- 服务端身份和资源授权；
- 幂等键；
- 速率与费用限制；
- 日志和审计；
- 对应前端流程和数据库操作。

#### `AUTH-AND-PERMISSIONS.md`

- 注册、验证、登录、会话续期、退出、重置；
- 身份与授权分开；
- 角色来源不能由用户自行修改；
- 每个受保护入口做服务端资源级检查；
- 数据库权限作为最后防线；
- Cookie/Token、CSRF、CORS 和回调地址；
- 管理员升降权和审计。

#### `ERROR-AND-LOGGING.md`

- 稳定错误代码与用户友好信息；
- 客户端不显示堆栈、SQL、内部路径；
- 日志包含请求 ID 和必要上下文；
- 日志必须脱敏，不记录 Cookie、Authorization、密码、Key 和完整隐私请求；
- 错误分级、告警和调查步骤。

#### `INTEGRATIONS.md`

对支付、邮件、短信、AI、文件、Webhook 和 Cron，仅在能力存在时记录：

- 触发者、负责人和审批；
- 密钥边界；
- 请求/响应 Schema；
- 原始请求体验签；
- 幂等、重试、退避、超时和失败队列；
- 限流、预算和 kill switch；
- 外部失败后的安全状态。

#### `BACKEND-TEST-PLAN.md`

- API 正常与验证失败；
- 未登录、错误角色、错误资源所有者；
- 重复、并发、超时、第三方错误；
- Webhook 伪造、重放和乱序；
- 日志脱敏、缓存隔离和费用限制。

### 7.3 后端设计确认门

AI 必须证明每个 API 能追溯到已完成的前端行为和已实现的数据库操作，没有孤立接口、重复业务规则或权限缺口。用户批准并归档后进入后端编码。

---

## 附录 C：阶段 11 集成与总审查的详细要求（当前不要执行）

### 8.1 目的

三层各自完成设计和编码后，AI 要检查最终实现是否共同满足同一份 PRD。本附录不能被用来阻止已获批的前端先行编码，也不能提前开展数据库或后端设计。

### 8.2 追踪矩阵

创建 `docs/02-planning/REQUIREMENTS-TRACEABILITY.md`：

| 需求 ID | 前端页面/状态 | 数据实体/约束 | 后端入口/权限 | 计划测试 | 状态 |
|---|---|---|---|---|---|

每项 Must 必须完整贯通；任何空格都属于阻断项。

### 8.3 冲突审查

- 名称和业务状态是否一致；
- 前端是否要求数据库无法安全支持的数据；
- API 是否返回过多字段；
- 身份、角色和资源归属是否在三层一致；
- 删除、退款、上传、AI 和消息的失败状态是否一致；
- 缓存、重试和幂等是否造成重复副作用；
- 测试是否覆盖钱、隐私、权限和数据完整性；
- 成本和免费额度是否仍满足章程；
- 是否出现不必要的依赖和架构复杂度。

### 8.4 更新项目手册

把已批准的前端规范、数据库规则和后端边界同步到 `PROJECT-HANDBOOK.md` 和 `AGENTS.md`。这次更新也必须经过用户确认。

### 8.5 审查修复任务书

更新 `docs/06-development/TASKS.md`。审查发现必须按依赖顺序修复，每项包含：需求 ID、影响层、范围、非范围、输入文档、预计文件、验收、测试、风险、回滚和归档点。

### 8.6 发布候选许可

阶段报告最后必须明确询问：

> 前端、数据库、后端的设计与编码、集成和总审查均已完成。是否批准 Git 归档，并在归档后进入质量与安全验收？

只有用户明确批准并完成归档后，才允许进入质量与安全验收。

---

## 10. 阶段 6：前端编码

### 9.1 边界

只实现已批准的页面、组件、状态、交互和前端校验。全部使用类型化合成数据与 Mock/Fixture 模拟后端；不得连接真实数据，不得临时创建数据库表或后端接口。

### 9.2 实现顺序

1. 按唯一技术栈初始化项目脚手架；
2. 只安装技术决策已批准的依赖；
3. 建立设计 Token 和基础组件；
4. 实现布局、导航和页面状态；
5. 实现表单与客户端体验校验；
6. 建立 Mock 数据适配层；
7. 编写单元/组件/可访问性/响应式检查；
8. 用浏览器验证核心流程。

### 9.3 安全底线

- 浏览器中没有秘密和服务端管理员逻辑；
- 不渲染未经安全处理的不可信 HTML；
- 不使用 `eval`、字符串代码或任意用户 URL；
- 不把长期会话秘密放 Web Storage；
- 前端权限判断只用于体验；
- 上传类型检查不能被当作最终安全检查；
- 错误不泄露内部细节。

### 9.4 子阶段归档

设计系统、页面骨架、核心流程、错误状态等可分别成为子阶段。每一个都要先汇报、确认、再提交。只有所有 Must 页面、状态、响应式、可访问性和前端测试都完成并经用户确认归档后，才进入数据库设计。

---

## 11. 阶段 7：数据库设计

前端编码全部完成并归档后，才执行附录 A 的全部要求。数据库设计以已运行的前端和 Mock 契约为输入，逐条验证页面真正需要的数据、权限和查询；不得要求前端适配一个未经验证的“理想数据模型”。

若数据库事实要求修改已完成前端，必须提交影响报告，退回前端变更流程，获得用户确认并重新完成相关前端测试。数据库设计报告获批并归档前，不创建数据库、不运行迁移。获批后进入数据库编码。

---

## 12. 阶段 8：数据库编码

### 10.1 边界

只实现已批准的 Schema、迁移、约束、权限策略、种子测试数据和数据库测试。不得提前实现后端业务接口。

### 10.2 实施规则

- 所有结构变化进入 `database/migrations/`；
- 能在全新测试数据库按顺序重放；
- 必填、唯一、外键、CHECK 和状态不变量尽量由数据库保证；
- 为真实访问路径建立索引；
- 默认拒绝，逐项授予权限；
- 测试用户 A 无法读写用户 B；
- 管理员能力最小化并可审计；
- 建库、迁移、种子、演示和测试全部使用合成数据；
- 禁止连接、复制或导入真实数据，即使只是为了“方便测试”；
- 破坏性迁移必须单独获得用户明确授权；
- 记录备份、恢复和回滚限制。

数据库编码每个迁移组都执行汇报确认归档。全部约束、权限、迁移重放、备份恢复和合成数据测试通过后，才进入后端设计。

---

## 13. 阶段 9：后端设计

数据库编码全部完成并归档后，才执行附录 B 的全部要求。后端契约必须同时服从已经完成的前端行为和已经运行的数据库结构，优先解决二者之间的真实连接，不得重新发明业务模型。

后端设计仍只使用合成请求、合成账号和测试环境。设计报告获批并归档前，不实现正式 API 或外部集成；获批后进入后端编码。

---

## 14. 阶段 10：后端编码

### 11.1 边界

只实现已批准的后端入口、服务、数据访问和外部适配器。不能为了“方便实现”改变已批准页面和数据库；需要改变时走变更控制。

### 11.2 每个入口必须检查

- 输入在运行时验证，类型声明不等于验证；
- 身份在服务端确认；
- 对具体资源做授权；
- 数据库查询参数化；
- 用户私有响应不进入跨用户共享缓存；
- 状态变化和外部副作用可幂等；
- 频率、大小、时间和费用有限制；
- 错误对用户安全、日志对运维有用且脱敏；
- 第三方超时、重试和失败状态明确；
- Webhook 在解析和执行副作用前使用原始请求体验签。

每个服务或接口子阶段都先汇报、确认、归档。全部使用合成数据和沙箱第三方服务；全部通过后进入集成与总审查。

---

## 15. 阶段 11：集成与总审查

### 11.1 替换 Mock

前端通过已批准的数据访问接口逐步替换 Mock。禁止组件直接绕过后端契约访问特权数据。联调数据仍然全部是合成数据；此阶段不得因“系统已经能跑”而导入真实数据。

### 11.2 全链路必测

- 注册、验证、登录、续期、退出、重置；
- 核心新增、读取、修改、删除；
- 加载、空、失败、无权限和恢复；
- 用户 A 对用户 B 的读写删除全部拒绝；
- 普通用户直接调用管理员入口拒绝；
- 修改 owner、role、price 等敏感字段无效；
- 重复提交和并发安全；
- 第三方超时、错误、重放和乱序；
- 手机、键盘和可访问性；
- 文档与实现追踪矩阵一致。

### 11.3 完成定义

“页面能打开”不是完成。必须有当前测试命令输出、真实浏览器验证、权限负面测试和文档同步证据。

### 11.4 总审查

执行附录 C 的需求追踪、跨层冲突和实现一致性审查，并补充：实现是否偏离设计、是否存在未记录依赖、公开仓库风险、真实数据导入风险、部署与回滚是否可执行。所有阻断问题修复后，向用户汇报并申请进入质量与安全验收。

---

## 16. 阶段 12：测试、质量和安全审查

### 12.1 测试映射

创建 `docs/07-quality-security/TESTS.md`，严格分为：

- 当前仓库已存在并实际运行的覆盖；
- 计划测试；
- 完全没有验证的缺口。

每项包含需求、规则、正常和拒绝结果、证据文件、CI 状态。计划测试不能写成完成。

### 12.2 安全审查顺序

1. 密钥和前后端边界；
2. 所有服务端入口的认证和资源授权；
3. 数据库/存储权限；
4. 输入验证、XSS、注入、CSRF、CORS、SSRF、重定向；
5. 上传；
6. 用户私有缓存和日志；
7. Webhook、幂等和重放；
8. 支付金额和订单事实来源；
9. AI、消息、Cron 的费用和滥用；
10. 依赖、构建和供应链；
11. 备份、恢复和事故响应。

安全报告每个发现包含：ID、严重度、精确证据、影响、修复、缓解和误报核对。Critical/High 未解决时阻断发布。

### 12.3 生产构建与发布候选

使用技术栈的真实命令运行：格式/规范检查、类型检查、单元、集成、E2E、依赖审计和生产构建。记录完整退出码和失败数。不能用某一项通过推断全部通过。

安全审查开始后冻结新功能，只修发布阻断问题；每个修复继续执行子阶段确认和 Git 归档。

本阶段以及之前所有阶段只能使用合成数据。只有测试、质量、安全、生产构建和总审查全部通过，用户批准“项目功能完成”并完成归档后，才允许进入真实数据就绪阶段。

---

## 17. 条件功能安全规则

### 18.1 文件上传

- 服务端验证数量、大小、类型和内容；
- 服务器生成文件名，不信任用户路径；
- 私密文件不放公开目录；
- HTML/SVG 等主动内容默认下载而非内联；
- 下载重新鉴权；
- 配额、恶意文件、删除和孤立文件有策略。

### 18.2 支付

- 优先托管支付页面，应用不处理卡号；
- 测试与生产密钥、商品和 Webhook 完全分离；
- 金额由服务端可信商品数据决定；
- Webhook 原始请求体验签；
- 事件 ID 唯一，重复通知不重复发货；
- 支付事实来自已验证通知，不来自浏览器成功页；
- 测试失败、拒付、退款、争议、乱序和延迟；
- 上线前确认费用、税务、退款和地区规则。

### 18.3 AI

- Key 仅在服务端；
- 输入、检索内容和输出均不可信；
- 工具使用严格允许列表、参数 Schema、服务端权限和批准门；
- 模型输出不直接变成 SQL、HTML、Shell 或高风险动作；
- 数据最小化，不发送无必要个人信息；
- 请求、用户、每日预算、超时和 kill switch；
- 高影响决定人工复核；
- 模型或 Prompt 变化后运行固定评测集。

### 18.4 邮件和短信

- 必要的用户同意和退订；
- 验证码短时有效并限制发送与尝试；
- 模板变量校验；
- 不发送密码或高度敏感信息；
- 按用户、IP、号码和全局预算限流；
- 重试幂等，不重复发送；
- 试用额度不被当作长期免费方案。

### 18.5 定时任务和后台任务

- 入口鉴权；
- 幂等键和并发锁；
- 每批上限、超时、重试和失败队列；
- 最后运行状态和告警；
- 手动重跑步骤；
- kill switch；
- 容忍延迟和漏跑。

---

## 18. 阶段 13：真实数据就绪与受控导入

### 13.1 何时允许开始

必须同时满足：前端、数据库、后端和集成全部完成；质量与安全验收通过；Critical/High 问题为零；备份与恢复已演练；用户已批准“项目功能完成”和本地归档。少一项都继续使用合成数据。

### 13.2 真实数据不是 Git 内容

- 真实数据永远不得提交到 Git 或 GitHub，包括历史提交、Fixtures、日志、截图、数据库导出和附件；
- 公开仓库只存代码、迁移、空 Schema、合成种子和脱敏示例；
- 密钥只放部署平台的秘密管理中，不写入仓库；
- 如果误提交过真实数据或秘密，仅从最新文件删除不够：必须暂停公开发布、清理历史并轮换秘密；
- 公共展示数据也要确认版权、许可证、隐私和再发布权。

### 13.3 数据就绪文档

创建 `docs/08-release/REAL-DATA-IMPORT.md`，至少包含：

- 数据来源、所有者、使用授权和允许公开的范围；
- 公开、内部、个人、敏感数据分类；
- 目标字段映射、必填、去重、格式、时区和编码；
- 删除不必要字段、脱敏和最小化规则；
- 导入前备份、校验和、记录数和抽样检查；
- 小批量试导入、失败回滚、重复执行幂等；
- 导入后数量、关联、权限和页面抽样验证；
- 日志脱敏和临时文件销毁；
- 数据更正、删除、导出和保留期限。

### 13.4 导入门禁

AI 先用结构相同的合成数据完整演练导入。随后提交报告，列出精确数据源、目标环境、预计记录数、公开可见范围、备份、命令、验证和回滚。用户必须明确批准这一次真实数据导入；批准归档不等于批准导入。

如果真实数据包含个人、客户、财务、健康、未成年人、访问凭据或其他敏感内容，默认不得放进任何公开页面；必要时应先取得适用地区的法律、隐私或行业专业审查。

如果产品的真实数据应由未来用户自行填写，则此阶段不预装真实数据，只验证空生产库和用户首次创建流程。

---

## 19. 阶段 14：GitHub 公开发布与上线

### 14.1 固定交付结果

最终必须同时得到：

1. 一个 GitHub **Public** 仓库，任何人都能查看和克隆代码；
2. 一个无需加入 GitHub、无需开发环境即可在浏览器直接打开的公开体验 URL；
3. 仓库 `README.md` 顶部清楚提供体验链接、功能说明、截图、运行方法、技术栈、许可证和已知限制。

公开 GitHub 仓库本身通常只是代码托管，并不会自动让动态应用可运行。静态项目可在技术决策证明合适时使用 GitHub Pages；需要后端、数据库或服务端秘密的项目，必须使用唯一技术栈所选的托管平台，并从 GitHub Public 仓库触发部署。

### 14.2 环境分离

至少区分 Local、Preview/Test、Production。预览不得连接生产数据库，自动测试不得发送真实消息、真实扣款或消耗无上限 AI 费用。

### 14.3 上线前产物

- `docs/08-release/ENVIRONMENT-MATRIX.md`；
- `VARIABLES-AND-SECRETS.md`：只写名称、范围、来源、轮换和风险，不写真值；
- `DEPLOYMENT-PLAN.md`；
- `DATABASE-MIGRATION-PLAN.md`；
- `ROLLBACK-PLAN.md`；
- `RELEASE-CHECKLIST.md`；
- `RELEASE-RECORD.md`。
- `PUBLIC-REPOSITORY-REVIEW.md`：许可证、秘密扫描、隐私/版权、历史检查、README 和公开风险；
- `PUBLIC-URL-CHECK.md`：未登录访客、手机、核心流程、错误页、性能和可访问性证据。

### 14.4 公开仓库检查

- 仓库可见性将设为 Public，精确仓库名和所有者已向用户展示；
- 完整 Git 历史而不只是当前文件经过秘密与敏感数据检查；
- `.env*`、密钥、数据库导出、真实数据、日志、私密上传和构建缓存均未跟踪；
- 开源许可证与依赖许可证已确认；没有授权不明的图片、字体、代码或数据；
- README 不承诺未实现能力，不暴露内部地址或运维秘密；
- 管理入口、调试模式、测试账号和开发后门未公开；
- 合成演示数据明确标注为示例，真实数据不会进入仓库。
- 在 GitHub 可用范围内启用并检查 Dependabot、Secret scanning、Push protection 和 Code scanning；这些自动检查是补充，不能代替发布前的本地历史审查。

### 14.5 发布顺序

```text
冻结范围
→ 备份与恢复核对
→ 生产配置复核
→ 测试环境迁移演练
→ 部署 Preview
→ 冒烟与权限负面测试
→ 展示 GitHub Public 仓库、公开 URL 和外部动作清单
→ 用户批准公开仓库创建/可见性与生产外部操作
→ 推送到 GitHub Public 仓库
→ 兼容数据库迁移
→ 部署应用
→ 生产冒烟
→ 从未登录浏览器打开公开 URL 验证
→ 把公开 URL 写入 GitHub README
→ 提交更新后的 README 与发布记录
→ 用户批准后再次 push，使公开仓库显示体验入口
→ 观察指标和成本
→ 全量或回滚
```

### 14.6 外部授权

用户已经指定最终形态必须是 GitHub Public 仓库和公开体验 URL，但 AI 执行前仍须展示精确仓库、账号、可见性、将公开的完整内容、托管目标、费用和回滚方式，取得本次明确授权。阶段归档确认不等于授权创建仓库、改变可见性、推送、部署、域名、付费资源或生产迁移。

### 14.7 回滚触发

- 跨用户数据访问；
- 错误扣款或重复副作用；
- 核心流程不可用；
- 错误率/延迟突升；
- 费用异常；
- 迁移导致数据不一致。

代码回滚不等于数据库回滚。数据库可能需要前向修复或备份恢复，必须按已批准方案执行。

---

## 20. 阶段 15：运营维护

### 20.1 必须文档

- `docs/09-operations/RUNBOOK.md`；
- `MONITORING.md`；
- `INCIDENT-RESPONSE.md`；
- `BACKUP-RESTORE.md`；
- `COST-CONTROL.md`；
- `DATA-RETENTION-DELETION.md`；
- `MAINTENANCE-SCHEDULE.md`；
- `RETROSPECTIVE.md`。

### 20.2 观察内容

- 核心任务成功率；
- 错误率、延迟和可用性；
- 登录失败、权限拒绝和管理员操作；
- Webhook、消息和后台任务失败；
- 数据库、存储、函数、AI、邮件、短信费用；
- 用户反馈对应的真实行为，不只看访问量。

### 20.3 维护节奏

每日/使用后：核心错误和异常费用。  
每周：产品指标、第三方失败、风险和用户卡点。  
每月：依赖公告、管理员/密钥审计、恢复抽查、数据删除、费用复盘。  
每季度或重大变更：完整安全、权限、备份恢复和项目手册审查。

### 20.4 事故

- P0：数据泄露、密钥泄露、错误扣款——立即止损、撤销密钥、限制入口、保留证据；
- P1：核心功能大面积不可用——回滚/降级并沟通；
- P2：局部功能失败且有替代路径——限定影响并修复；
- P3：轻微体验问题——正常排期。

先恢复安全状态，再查根因。事故记录只写去敏事实，不清理证据，不把“人为失误”当最终根因。

### 20.5 备份与退役

只有实际恢复过的备份才可信。项目关闭时：停止写入、通知用户、提供导出、撤销 Webhook/密钥、删除云资源、按政策删除数据和备份、停止计费、保留不含秘密和个人数据的代码文档复盘。

运营中的每个变更仍需：读项目手册 → 规划/设计影响 → 实施验证 → 阶段汇报 → 用户批准 → Git 归档。

---

## 21. 系统化排错规则

遇到 Bug：

1. 写出最短稳定复现；
2. 读取完整错误、日志、版本和最近差异；
3. 判断在前端、数据库、后端、第三方还是环境；
4. 找仓库中相似且正常的对照；
5. 提出单一根因假设；
6. 做只改变一个变量的最小实验；
7. 建立能捕获原症状的回归测试；
8. 只修根因；
9. 重跑直接测试、完整检查和原复现；
10. 更新测试、Runbook、事故或项目手册。

同类修复连续三次失败，停止叠加补丁，重新审查需求、假设或架构，并向用户报告。

Bug 修复也属于子阶段，仍需先汇报、确认、再 Git 归档。

---

## 22. 变更控制

### 22.1 什么时候必须退回设计

以下变更不能在编码中直接实现：

- 新角色、新权限或数据共享；
- 新页面/核心流程或重大交互变化；
- 新表、关系、数据保留或删除行为；
- 新 API、外部服务、支付、AI 工具或自动副作用；
- 公共接口、域名、缓存、部署或费用模型变化；
- 生产依赖或技术栈变化。

### 22.2 退回顺序

- 影响用户体验：退回前端设计，再重新检查数据库和后端；
- 影响数据模型：退回数据库设计，再重新检查后端和前端契约；
- 只影响服务端实现且不改变契约：退回后端设计；
- 改变技术栈：退回技术决策和项目手册。

任何重新设计都需要新的阶段报告、用户确认和 Git 归档。

---

## 23. 给零基础用户的学习要求

每个子阶段报告之后，AI 额外提供：

```text
【本阶段学习卡】
1. 你刚刚完成了什么：
2. 为什么项目需要它：
3. 三个新概念：
4. 最容易踩的两个坑：
5. 你可以亲手做的一个小练习：
6. 如果出错，先看哪里：
```

建议学习顺序：文件与终端 → Git → Web 请求与响应 → 前端组件和状态 → 数据表与约束 → 身份与权限 → 后端接口 → 测试与 CI → 部署、日志、备份和事故。

---

## 24. 新项目启动提示词

用户不需要先创建目录、执行 Git 命令或复制文件。打开一个能够访问本机文件与终端的 Codex 本地任务，直接发送下面这段话。除非用户指定其他位置，Windows 默认项目父目录使用 `C:\Users\10342\Documents\Codex\Projects`。

如果模板包已移动，只需把提示词中的“模板 ZIP 路径”替换成新位置。用户唯一需要填写的是项目称呼；不知道叫什么时保留“请你命名”即可。

```text
我想启动一个新的 Vibe Coding 项目。请你尽可能自动完成启动操作，不要让我手动创建文件夹、复制模板或输入 Git 命令。

【我的输入】
- 项目想法：目前没有明确方向，请在仓库启动完成后按立项阶段帮助我探索。
- 项目文件夹名：请你根据项目情况生成；如果现在信息不足，先使用 vibe-project 加日期时间，并在创建前告诉我。
- 默认项目父目录：C:\Users\10342\Documents\Codex\Projects
- 模板 ZIP 路径：C:\Users\10342\Documents\Codex\2026-08-30\new-chat\outputs\vibe-coding-beginner-kit.zip

【启动授权】
我授权你在上述父目录内创建一个新的、独立的项目文件夹，初始化本地 Git 仓库，并把模板包内容复制到项目根目录。这份授权仅包含可恢复的本地启动动作，不包含 git add、git commit、git push、创建 GitHub 仓库、部署、安装技术栈、创建付费资源或使用真实数据。

请按以下顺序自动执行：

1. 只读检查 Git 是否可用、模板 ZIP 是否存在，以及默认父目录是否可访问。
2. 生成合法的英文小写短横线项目目录名，并向我显示最终绝对路径。若目标不存在，继续；若目标已经存在且非空，停止并只问我一个必要问题，绝不覆盖。
3. 创建父目录和新项目目录。
4. 确认新项目目录为空，然后在该目录执行 git init，并将初始分支设置为 main。
5. 安全读取模板 ZIP，把 ZIP 内模板根目录的“内容”复制到新项目根目录。不要把外层 vibe-coding-beginner-kit 再套一层，不要复制任何 .git。
6. 验证新项目根目录直接包含 VIBE-CODING-AI-GUIDE.md、AGENTS.md、README.md、docs、prompts 和 checklists。
7. 完整读取新项目根目录中的 VIBE-CODING-AI-GUIDE.md、AGENTS.md、README.md 和 docs/README.md。
8. 根据模板创建 docs/00-governance/PROJECT-STATE.md，记录阶段 0、精确项目路径、当前状态和下一门禁。
9. 检查 .gitignore、目录结构、Git 分支、git status、秘密、真实数据、错误嵌套和意外文件。
10. 不安装框架，不选择技术栈，不生成应用脚手架，不写业务代码，不执行 git add、git commit、git tag 或 git push。
11. 最后向我提交阶段 0 完成报告，说明实际创建路径、实际执行命令、文件清单、验证结果、风险和建议首次提交消息，并询问我是否“批准归档”。

AI 代办规则：凡是你能通过当前文件和终端工具安全完成的本地操作，都由你直接完成，不要改成教程让我手动做。只有遇到真实权限阻止、账号登录、验证码、Codex 必须由我点击选择项目文件夹，或者会改变产品/数据/费用的决定时，才让我介入。介入时一次只告诉我一个最小动作，解释原因，并在我完成后继续自动处理。

如果当前 Codex 任务无法自动切换到新目录，但目录和仓库已经成功创建，请先完成所有能在绝对路径上完成的检查，然后只告诉我：在 Codex 中选择哪个精确文件夹。不要让我重新创建目录或重新执行 Git 命令。

新项目建立后，请严格按新项目根目录中的手册状态机主持后续工作。

我是零基础用户。先执行“每次新会话开工协议”，检查当前仓库，但不要选择技术栈、安装依赖、生成脚手架或写业务代码。若 PROJECT-HANDBOOK.md 尚不存在，这是正常的；它应在立项和唯一技术栈决策完成后创建。

从阶段 0 开始，一次只推进一个子阶段。每个子阶段结束时必须先按规定向我提交完整报告和学习卡，询问是否批准本地 Git 归档。在我明确回复“批准归档”之前，不得执行 git add、git commit、git tag 或 git push。归档完成后再进入下一子阶段。

技术栈必须在立项后，依据项目文档、当前官方资料和优秀同类案例，使用明确权重评估并选择唯一最适合本项目的一套，不要给我多个选项让我自行决定。

正式代码开始前，必须先完成并让我确认：项目专属 PROJECT-HANDBOOK.md、总体规划、设计规范、代码架构规则、安全基线和测试策略。随后严格按“前端设计并确认 → 前端全部编码完成 → 数据库设计并确认 → 数据库全部编码完成 → 后端设计并确认 → 后端全部编码完成 → 集成和总审查”推进，任何阶段不得偷跑。

项目功能、测试、集成、质量和安全验收全部完成前，只能使用合成数据。验收通过后，按真实数据就绪阶段另行报告并等待我批准导入；真实数据永远不得进入 Git。最终发布目标固定为 GitHub Public 仓库加一个任何人可直接打开的公开体验 URL，但实际创建仓库、改变可见性、push 和部署前仍要展示精确目标并取得我的明确授权。
```

### 24.1 实际对话应该有多简单

```text
用户：
请按上面的启动提示词自动创建新项目。我没有方向，项目名也请你处理。

AI：
我会在已授权的默认父目录中创建一个新的独立目录。拟用目录名为 vibe-project-20260830-1430，完整路径为……。我将自动完成目录创建、Git 初始化、模板复制和阶段 0 检查；不会提交、推送、安装框架或写业务代码。目标路径不存在，可以继续。

（AI 使用文件和终端工具自动执行，不要求用户输入命令。）

AI：
【阶段 0 完成报告】
实际项目路径：……
Git：已初始化，main 分支，无提交
根目录：主手册和模板均已正确放置，无多层嵌套
验证：……
建议提交：chore: initialize project workflow
是否批准本子阶段进行本地 Git 归档？

用户：
批准归档。只允许本地提交，不要 push。

AI：
（AI 自动执行精确暂存、检查和本地提交，然后报告 commit ID，并进入立项前的下一个确认点。）
```

如果 AI 在具备工具权限时仍让用户执行 `mkdir`、`cd`、`git init`、解压和复制等机械步骤，用户可以回复：

```text
请遵守手册的“AI 代办优先原则”。能由你的文件和终端工具安全完成的已授权本地操作，请直接完成。只有遇到真实权限或必须由我操作的界面时，才向我请求一个最小动作。
```

---

## 25. 参考依据

流程吸收规格驱动、证据优先、安全开发生命周期和可审计文档思想。执行时，所有可能变化的版本、价格、许可、安全建议和平台限制必须重新核查当前官方资料。

- GitHub Spec Kit：<https://github.com/github/spec-kit>
- GitHub Vibe Coding 教程：<https://docs.github.com/en/copilot/tutorials/vibe-coding>
- GitHub 仓库可见性及公开后果：<https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/managing-repository-settings/setting-repository-visibility>
- GitHub Pages 适用范围（静态站点）：<https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages>
- GitHub 公开仓库安全分析：<https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/enabling-features-for-your-repository/managing-security-and-analysis-settings-for-your-repository>
- OpenAI Developers / Codex：<https://developers.openai.com/>
- Codex 集成终端：<https://learn.chatgpt.com/docs/integrated-terminal>
- Codex 本地环境：<https://learn.chatgpt.com/docs/environments/local-environment>
- Codex 操作批准与安全：<https://learn.chatgpt.com/docs/agent-approvals-security>
- OpenAI 模型提示与授权边界：<https://developers.openai.com/api/docs/guides/latest-model>
- OWASP ASVS：<https://github.com/OWASP/ASVS>
- OWASP SAMM：<https://owaspsamm.org/>
- OWASP Cheat Sheet Series：<https://cheatsheetseries.owasp.org/>

高星案例是研究入口，不是正确性证明。最终工程事实必须由项目需求、官方文档、实际代码、测试输出和运行证据共同支持。
