{
  "name": "tracepbl",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "database",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run typecheck && npm run build --workspace @tracepbl/web",
    "db:migrate": "npm run migrate --workspace @tracepbl/database",
    "db:seed": "npm run seed --workspace @tracepbl/database",
    "db:test": "npm run test --workspace @tracepbl/database",
    "dev": "npm run dev --workspace @tracepbl/web",
    "dev:api": "npm run dev --workspace @tracepbl/api",
    "dev:worker": "npm run dev --workspace @tracepbl/worker",
    "local:up": "docker compose up --build",
    "local:down": "docker compose down",
    "format:check": "git diff --check",
    "lint": "eslint tests && npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:e2e": "vitest run tests/backend.e2e.test.ts",
    "test:integration": "npm run test:integration --workspace @tracepbl/database && npm run provision:runtime --workspace @tracepbl/database && npm run checkpointer:init --workspace @tracepbl/worker && npm run test:integration --workspace @tracepbl/api && npm run test:integration --workspace @tracepbl/worker",
    "typecheck": "tsc -p tests/tsconfig.json --noEmit --pretty false && npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=24.20.0",
    "npm": ">=11.19.0"
  },
  "devDependencies": {
    "eslint": "10.9.1",
    "typescript": "6.0.3",
    "typescript-eslint": "8.68.0"
  }
}
