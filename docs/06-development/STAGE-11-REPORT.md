# 阶段 11 前后端集成报告

日期：2026-09-05。状态：集成及本地/Demo 预览已确认，用户已批准本次 96 路径本地归档。阶段 12 未开始。

## 1. 基线与授权

本阶段是工程集成，P11 页面保持取消。Git 基线 `29d1591`：P00 最终视频 25 路径归档；前端主归档 `0d8d898`、状态收口 `3c708b8`、数据库设计 `51270a8`、数据库实现 `db19d4f`、后端设计 `d688524`、后端实现 `927a90b`。初始工作区只有既有未跟踪 `output/`。

用户已授权阶段 11 代码、测试和本地验证，并批准 CP-11-01/02/03。没有授权 Git 暂存、提交、tag、push、部署、真实模型、读取本机密钥或进入阶段 12。所有文件仍在工作区，暂存区为空。

## 2. 已实现内容

| 主链 | 实际实现 |
|---|---|
| 教学情境与问题 | 保存真实课程字段；支持可选课型、单问题零子项、整课 2—4 子项；按服务端版本恢复 |
| 来源与证据 | 同 task 来源目录、旧版本保留、元数据与限制展示、材料输入、证据关系和引用；未知不冒充核验 |
| 活动与量规 | 按栏目生成并直接填入可撤销草稿；教师编辑后一次确认保存，单项替换保留其他项目；上游变化传播待复核 |
| 后台任务 | PostgreSQL job、Worker、fake provider、LangGraph checkpoint；SSE、任务列表恢复、取消、当前修订重新发起、过期结果拒绝 |
| 审计与终审 | 当前修订检查、缺项/引用/对齐/课时/权利门禁、教师采用和签发；理由原文读取、编辑追加和失败恢复已接通 |
| 导出 | 同一次签发的冻结正文、来源版本和引用；前端生成实际 Word/PDF；受限来源只输出安全替代 |
| 两种模式 | Demo 保持 IndexedDB 和公开预生成数据；complete 模式使用同源 localhost API，任务数据不进入 IndexedDB |
| 运行与 CI | Compose 增加完整前端，修复初始化和 Node 运行方式；CI 加入阶段 11 跨层用例 |

新增 0004，不改写 0001—0003 或已有历史。无新生产依赖；Web 仅复用已有 workspace contracts 包。GLM-5.3-Flash、embedding-3/1024/cosine 保持不变，本轮实际运行 fake。

## 3. 有效验证

前期有效证据：真实数据库 18、最小权限 API 6、checkpoint 1、后端 E2E 1，以及未受影响非数据库用例均保留。CP-11-03 最新 Web 32/32、Worker 单元 6/6、跨层 13/13；另 1 checkpoint 本轮单元命令跳过，不计入本轮通过。全仓 typecheck、lint 与 complete 构建通过。OpenAPI 3.1 快照包含 24 个路径与 reviewedContent，服务器入口为 localhost。

完整浏览器主链已实测四类建议的审阅/采用、服务器检查、终审、Word/PDF 下载和 SSE 刷新/断网恢复。静态生产 Demo 实测课程保存、刷新恢复及 0 API/外部请求。屏宽截图曾捕获加载态，已加强等待正文条件并重跑通过；三种宽度无横向溢出，390px 完整正文截图已人工核对。

具体命令、失败和重试后通过原因见 [验证记录](./STAGE-11-IMPLEMENTATION-VALIDATION.md)；复验入口见 [本地运行说明](./STAGE-11-LOCAL-RUN.md)；解释见 [学习卡](./LEARNING-CARD-11.md)。

## 4. 变更关闭与遗留风险

**用户 14 条 Demo 批注已处理。** 课型默认展开且仍可跳过；学情通用化、选择块重排；方向和最终交付增加可编辑选项；史料架、活动、审计和最终预览统一字号、间距与纸色。修复 P02 补方向时输入区提前切换及刷新恢复问题。六页 × 三种宽度无横向溢出，方向沿用、跳过、自由编辑和刷新已实测。详见验证记录末节；这是当时的 Demo 验证记录；当前完整容器已更新，Demo 后续保持用户确认版本。

**CP-11-02 已批准并完成。** `teacherReason: string | null` 只投影同 workspace/task/finding 最新教师决定原文；修改追加不可变历史。跨层验证初始 null、原文恢复、编辑追加、重复键、跨任务、删除后及过期拒绝；浏览器验证修改后刷新恢复，以及保存失败后同键重试且不能跳过保存。没有新增数据库列、迁移或权限。

真实模型质量、真实历史材料适用性、真实费用、专家/教师试用、跨浏览器和辅助技术、Word 桌面视觉、远端 CI 与公网访问均未验证；本阶段授权不包含真实模型/数据/发布。pdfmake 大块构建警告保留，未降低阈值；阶段 12 的全量安全/质量结论不能由本报告代替。

## 5. 需求与证据追踪

| 需求 | 设计/实现入口 | 当前证据 |
|---|---|---|
| MUST-001/002 | 现役前端契约、context/question-set、0004 | 四问题与空课型测试；首次年级选择；真实浏览器问题采用 |
| MUST-003/004 | SourceRepository、P03、seed-integration、固定 profile | 私有来源 404、旧版保留、权利/条件状态、RAG 唯一 profile 修复；无联网发现声明 |
| MUST-005 | evidence-map、citation 约束 | 完整保存主链、冻结引用、跨 task 负向及来源变更回退 |
| MUST-006/007 | lesson-design/rubric、确定性 audit | 活动/量规采用与保存；时长含转场；上游复核；实际下载 |
| MUST-008/009 | WorkflowRepository、教师决定、export snapshot | 阻断不可接受、过期审核拒绝、冻结导出；教师理由读取/编辑/重试已验证 |
| MUST-010 | scoped repository、job functions、任务菜单 | workspace/task 404、删除立即隐藏、24h 恢复/过期、到期清理、私有副本 |
| NFR-001/011/012 | 来源版本、不可变 revision/decision、审计 | DB 不可变历史/角色、快照不跟随当前编辑；理由原文恢复和历史追加已验证 |
| NFR-002/003/010 | Demo/local 入口、会话、safe problem | 无公开后端、无 IndexedDB 完整任务、无外部浏览器请求、无真实数据/密钥 |
| NFR-004/005/009 | Worker/图/SSE/操作列表 | 租约与有限重试现有测试、取消/过期跨层、断网恢复；不自动复活终止 job |
| NFR-006/007 | 现役前端样式及响应式 | 本阶段只验证 Chromium 三宽度；其他人工/兼容项不宣称通过 |
| NFR-008 | 原有预算 ledger + fake | 保持限额；未开真实费用，合成测试不是实际账单证明 |

## 6. 精确路径清单和归档门禁

当前工作区路径见 [STAGE-11-ARCHIVE-PATHS.md](./STAGE-11-ARCHIVE-PATHS.md)。清单已根据最终工作区重新核对，共 96 个路径。不是 Git 操作授权；阶段完成报告获用户回复“批准归档”之后，才允许清单内路径本地暂存/提交。

`output/`、`.tracepbl/`、`.playwright-cli/`、本地凭据、容器卷和临时下载不归档。P00 视频/海报、旧迁移和未授权路径保持原样。

## 7. 本地预览

本轮静态 Demo：`http://127.0.0.1:5174/tracepbl/tasks/demo-tang-45m/context`。只有公开史料、合成教学情境和预生成建议，不连接后端。已更新生产构建；旧标签页需刷新载入新样式与交互。

完整模式：`http://127.0.0.1:5173/`；仅本机可访问，fake provider，无真实模型费用。已完成合成教学包可从“我的备课”继续打开。预览不授权归档，用户仍需单独回复“批准归档”。

本地入口故障修订已完成：PostgreSQL 重启策略、请求超时、等待提示和同键重试已补齐；前端测试现为 28/28（累计非 DB 79 项，其中未受影响证据保留）。真实浏览器点击进入教学情境，失败/丢响应重试仅新增一任务验证通过。详见验证记录末节。预览使用前台 WSL 终端，未建立后台常驻服务；关闭终端或休眠后可能需恢复本地环境。

## CP-11-03 当前完成状态

已按用户要求取消全局 AI 助手与独立采用面板：问题/子问题/交付、证据关系、活动和量规均在具体栏目放置按钮。生成直接填入草稿，可撤销、修改或替换单项，页底一次确认保存。教材、学情、材料原文、授权、核验与签发仍由教师负责。当前为 fake 合成示例，重新生成可能相同，不冒充真实 AI。

reviewedContent 最小接口补齐已批准并完成；原始生成历史保留，教师最终内容原子保存。连续生成、迟到结果、取消、过期、刷新恢复和保存丢响应重试均有实际验证。未确认手工编辑只在当前窗口；刷新可恢复服务器原始建议，离开前应确认保存手工修改。

本轮最新证据：Web 32/32、Worker 单元 6/6（另 1 checkpoint 用例跳过不计通过）、跨层 13/13；全仓 typecheck/lint、complete 生产构建及官方镜像完整启动通过。栏目交互 12 布局、恢复负向、完整 Word/PDF 导出与 SSE、本地材料/审计 9 布局均通过。旧有效数据库/安全证据保留，未把跳过项或此前失败当成通过。详见验证记录末节。

当前独立合成预览：`http://127.0.0.1:25173/`。原 5173 的现有任务和已用额度保留；5174 Demo 未重建。报告和精确清单仍待用户“批准归档”，没有 Git 暂存/提交、tag、push、部署或进入阶段 12。

量规预览列错位已修复：AI 按钮归入评价维度操作区，三级标准与表头对齐。新增四宽度、查看/编辑共八种实际布局断言，修复前失败、修复后通过；未更改数据或生成行为。精确归档清单现为 96 路径。

## 用户确认本地后的 Demo 同步

本地效果已获用户确认，随后用户授权同步静态 Demo。已同步栏目操作和量规列对齐，示例直接展示，无需触发 AI；可恢复示例、撤销和修改。初次演示检查含明确标注的预填合成理由，完整流程及 Word/PDF 下载、刷新恢复、四页三宽度和 0 后端/外部请求已实测。5174 Demo 生产构建已更新；此前“Demo 未重建”描述属于此前轮次。用户已有任务与 IndexedDB 未清空。本地后端规则、P00 成片和 96 路径归档门禁不变。


## 最终归档批准

用户于 2026-09-05 明确回复“都没问题了，批准归档”。授权最终清单所列 96 路径的一次本地提交；此前待批准状态为历史记录。本次仅补记批准与归档状态，保留已通过测试证据，不重复未受影响检查。output/ 保留；不包含 tag、push、部署、真实模型或阶段 12。
