# 前端深度优化实现验证

> 日期：2026-09-03
> 范围：阶段 6.13，P00—P09、P12、游客本机任务与真实本地导出

## 1. 实现合同

- 现役口径统一为高一第 6 课《从隋唐盛世到五代十国》，中心问题“唐朝为何由盛转衰？”。
- P04 核验并入 P03，P10 导出并入 P09，P11 取消且未补做。
- 不进入数据库/后端，不实现具体 API、真实 RAG、联网搜索、AI 或文件上传解析，不使用真实个人或学生数据。
- P03 换批只使用内置公开来源池并保留勾选；网址和文本入口不冒充抓取或 AI 解析。

## 2. 自动化证据

最终命令结果以本文件末次回填为准：

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：1 个整合合同文件，19 项测试全部通过。
- `npm run build`：Vite production build 通过，转换 56 个模块。
- `npm audit --audit-level=high`：0 个漏洞。
- `git diff --check`：通过；只有 Git 行尾转换提示，无空白错误。

最终构建：入口 JS 222.41kB/71.27kB gzip，CSS 119.11kB/21.27kB gzip；P09 页面 375.24kB/107.52kB gzip；`pdfmake` 为点击 PDF 后才加载的独立动态块 971.44kB/346.25kB gzip。大块提示已记录为风险，不影响首页首屏。

测试覆盖默认课程口径、详细史料节选与两段式解读、固定目录顺序、证据关系移动、七段 45 分钟活动、五维量规、非阻断上游变化提示和导出结构。

## 3. 真实浏览器证据

使用应用内真实 Chromium 验证：

- 首页刷新后的首个可读状态为 `currentTime=0`、`ended=false`，视频无旧版 `poster` 且旧静态后备为 `display:none`；随后从开头播放。播放结束仍定格最后一帧并显示重播按钮，不循环；鼠标移动后微光 CSS 坐标立即更新。
- P01、P02、P03、P05、P06、P07、P08、P09 桌面布局均完成截图检查；修复了旧负边距造成的活动/检查摘要重叠、量规首列过窄和任务菜单无完整样式。
- P03 深链载入 6 条默认史料；复选框与阅读按钮为并列控件，点击目录第二条成功切换阅读器，勾选切换前后 6 条目录标题顺序完全一致；阅读器依次显示“材料释义”和“可论证什么”。
- P05 三个问题均有证据时可继续，旧 `needs-review` 只显示统一提示，不要求逐条重复确认；“调整史料”复用次级按钮样式。
- P06 点击单个“修改”后一次出现时间、学生动作、史料、成果、卡点和教师支架；时间单位紧邻数值、史料选项尺寸一致。删除反馈与撤销使用统一状态条。
- P07 只保留上方一个“添加评价维度”；点击后页面由 `scrollY=0` 滚动定位至新维度，标题输入获得焦点且完整可见。活动变化只提示，不阻断继续。
- P08 完成重新检查与“采用建议”后进入 P09；已通过页可以自由回看。
- 桌面“我的备课”锚定右上角，点击外部关闭；390×844 下为底部抽屉。
- 390×844 检查 P02、P03、P05、P06、P07、P09；本轮复核 P03 页签、P06 编辑态和 P07 单一新增入口，三页 `scrollWidth` 均未超过视口，未观察到页面级横向挤压或导航位置跳动。

## 4. 导出交付物

在 P09 实际触发 Word 和 PDF 下载。修正后文件：

- `史证工坊-唐朝由盛转衰.docx`：最终下载副本 14,591B；OOXML 可读取，包含中心问题、材料释义、可论证内容、可读史料标题、0—5 分钟累计区间和五维量规，不包含 `SRC-DU-FU` 等内部史料 ID。
- `史证工坊-唐朝由盛转衰.pdf`：最终下载副本 139,671B；A4、5 页。PDF 文本层包含材料释义和可论证内容；5 页均已重新渲染并逐页目视检查，中文、来源、活动、累计时间、量规表和页码无缺字、截断或重叠。

当前机器未安装 Microsoft Word 或 LibreOffice，官方 DOCX 渲染脚本因缺少办公套件无法运行；因此只确认浏览器下载和 OOXML 结构，不虚报 Word/LibreOffice 视觉兼容。

## 5. 内容与安全边界

- 史料候选带 HTTPS 定位、机构、性质、节选、材料释义、可论证内容、局限和权利边界；艺术化首页视频不进入史料池，也不标记为历史复原。
- 本机任务、IndexedDB、步骤状态和按钮显隐均不构成真实授权；P12 继续返回等价不可用结果。
- 教育评价方向参照教育部《普通高中历史课程标准（2017年版2020年修订）》；具体量规是本课的可观察表现落地，不宣称为官方量规原文。

## 6. 剩余风险

- DOCX 仍需 Word 与 LibreOffice 真实打开；Safari/Firefox、读屏、强制高对比和 200%/400% 缩放仍需发布前设备矩阵。
- `pdfmake` 动态块与中文字体使首次 PDF 导出较重，但不进入首页首屏。
- IndexedDB 清理、隐私模式或配额可能造成本机历史丢失；界面已说明且不承诺同步。
- 历史学科内容仍需一线教师试教，演示数据不能替代教学验证。
