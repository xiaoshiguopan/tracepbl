# P00 最终影片视觉与交互验收记录

> 验收日期：2026-09-01
>
> 状态：MiniMax H3 首版候选已接入；下三分之一信息轨已通过技术与浏览器初检，等待用户视觉批准；物理移除音轨按用户决定后置
>
> 地址：本地 `/tracepbl/`；无部署、数据库、后端或真实数据

## 1. 影片生成证据

- 官方接口：MiniMax 中国区 `POST /v2/video_generation`，按官方 V2 首尾帧结构调用。
- 模型：`MiniMax-H3`；任务 ID：`437084928930074`；服务端最终状态：`succeeded`。
- 输入：`p00-film-keyframe-reveal-v1.webp` 为首帧，`p00-film-keyframe-matchcut-v1.webp` 为尾帧；两图均为 1672×941 的项目定制 AI 艺术关键帧，不是史料。
- 请求规格：2K、6 秒、`ratio=adaptive`、无 AIGC 画面水印。
- 下载文件：`p00-cinematic-h3-v1.mp4`，9,745,119B，SHA-256 `e5bb5d736e253e4037d205ae6225d0fea428c00bdd282c8266aaf7859b9d861f`。
- 浏览器实测：2560×1440、6.584 秒、H.264 视频；原始 H3 文件同时含 AAC 音轨。

密钥只从仓库外 `C:\Users\10342\Desktop\1.txt` 读取，没有输出、复制进项目或进入 Git 差异。该路径和密钥值不属于项目产物。

## 2. 视觉检查

| 时点 | 当前证据 | 结论 |
|---:|---|---|
| 0.26s | 王圆箓肩后背影、灯笼和藏经洞纵深与首帧一致 | 通过 |
| 2.43s | 镜头高速进入洞窟，近景形成运动模糊，非匀速静态缩放 | 通过 |
| 4.21s | 洞窟、壁面和实体工作台被揭示，镜头向纸面收束 | 通过 |
| 6.584s | 影片停在有实体支撑的展开纸面并保持尾帧 | 通过 |

未发现漂浮白纸、白洗、门户光圈、UI、水印、正面确定性肖像或可辨识馆藏信息。尾纸表面存在不可读的压纹式纹理；它不是可引用文字，但仍需用户在最终审美验收中确认是否接受。

## 3. 播放、响应式与可访问性

- 桌面 1440×900：视频自动播放成功，`muted=true`、无控件、只播放一次并停留在尾帧；标题、用途和入口组成贯穿底部安全区的一条信息轨。标题两行同字号同颜色；入口无完整描边、底色、阴影或循环边光。
- 390×844：不创建 `<video>`，使用独立竖版静态构图；横向溢出 0px。信息轨改为单列，标题 358×83px、用途 358×44px、入口 358×50px；顶部“演示说明”仍在视口内。
- `prefers-reduced-motion: reduce` 与窄屏共用静态路径，不创建影片元素。
- 影片和图片均为装饰层，不进入焦点或辅助技术顺序；标题、主动作和可展开“演示说明”由真实 HTML 提供。
- 单击一次 `进入史料工作台` 后到达 `/tasks/demo-tang-45m/context`，可见树中的页面一级标题为“教学情境”，没有第二次确认。

## 4. 当前验证

- ESLint：退出码 0。
- TypeScript：退出码 0。
- Vitest：2 文件、8 项全部通过。
- Vite production build：退出码 0；字体文件均复制到 `/tracepbl/assets/p00/`。六个标题字形切片合计 235,124B；无运行时外部字体请求。
- 浏览器媒体：`readyState=4`、自动播放、强制静音、2560×1440、6.584 秒。

## 5. 未完成门

1. 用户尚未对整段镜头的审美、冲击力和纸面纹理作最终批准。
2. H3 母版含 AAC 音轨。页面已经强制静音；用户决定视频仍可能修改，因此物理去音轨后置，当前不安装 FFmpeg。
3. 视频画面最终定稿后仍需无损复制视频流、删除音频流、复核哈希与浏览器播放，再把本记录状态改为最终通过。
4. 自动化浏览器已验证 DOM 语义和一次点击；当前控制接口未能可靠驱动 Tab 键，因此完整人工 Tab/Enter、缩放、高对比与读屏矩阵仍不宣称完成。
