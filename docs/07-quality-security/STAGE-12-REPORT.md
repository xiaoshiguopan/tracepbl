# 阶段 12 质量与安全报告

日期：2026-09-05。状态：本地审查与获批修复完成，用户已于 2026-09-05 明确“批准归档”，62 路径随本次本地提交归档。不推送、部署或进入阶段 13；本报告不是生产或真实数据批准。

## 改了什么

以阶段 11 归档 `97321fe` 为基线，保留确认的 Demo、原位 AI 交互、影片、来源契约与技术栈。完成十类已知安全发现的修复和相应验证；现有已知 Critical/High 未解决项为 0，结论限于本文列明的本地合成范围。

CP-12-01 已获批准。新增 0005 的两张恢复保护表、独立删除清单与恢复门禁：旧备份不能直接开放；清单或 checkpoint 清理失败时保持关闭，确认清理后任务数为 0、删除记录保留。旧数据升级需显式建立新基线，基线以前的备份不自动获得恢复资格。没有修改 0001—0004，没有在用户原预览库执行升级。

生成和 embedding 统一冻结价格并在调用前预留金额，已发起但结果未知时保留额度、不重复执行。恢复后真实调用至少关闭 24h，防止旧费用记录重置日限额；fake 继续零费用。真实 GLM 从未启用，未读取本机密钥。

其余修复包括私有 API no-store、短窗口限流、Vite 私有目录拒绝、映射 IP 拦截、真实 AI kill switch、CI Action 固定 SHA，以及模型响应分块大小限制和向量序号校验。普通 CRUD、引用、权限、删除与教师确认仍由确定性代码和数据库约束执行。

## 验证结果

| 检查 | 最终证据 |
|---|---|
| 单元测试 | 100 passed；7 个依赖 DB 的用例只在此命令跳过，已在集成执行 |
| 数据库 / API / checkpoint 集成 | 18 + 6 + 1 passed |
| 后端 E2E / 阶段 11 跨层 | 1 + 13 passed |
| 阶段 12 安全回归 | 8 passed，包含 13 类读取/SSE 入口逐一 foreign/missing 对照 |
| lint / typecheck / Vite production build | 最终 exit 0；保留大 PDF chunk 与混合导入警告 |
| 全量依赖 audit / 秘密模式扫描 | audit 0 漏洞；420 锁依赖有完整性，30 个可达提交及获批工作区路径高置信模式 0 命中；不宣称穷尽无秘密 |
| 真正 custom dump/restore | 旧库拒绝开放，合并独立最新清单后任务 0、保护记录保留；checkpoint-first 生产恢复核心执行成功 |
| 独立完整启动 | 最新缓存依赖候选 API/Worker/PG/Web 启动，ready 200；实际删除/幂等/撤销/no-store 通过 |
| Chromium | 原位生成、编辑确认、撤销、局部替换、丢响应同键重试、取消、断线恢复；四页 × 1774/390/320 无横向溢出 |

失败过程没有删去：新增 SQL 名称冲突、测试表数/测试 fixture/严格类型错误先记录再修复；最终只重跑受影响项。完整日期、命令、退出码与继承证据见 [验证记录](./STAGE-12-VALIDATION.md)。

## 仍未验证与保留风险

- 本轮 Docker Hub token 超时，**全新官方镜像拉取/无缓存安装不算通过**。代码、构建与完整启动使用既有官方阶段 11 依赖层的当前代码镜像验证，没有换镜像源。
- Windows 原生 PowerShell + PostgreSQL 客户端整套恢复没有执行；脚本语法检查和 Linux PostgreSQL 18.6 真正 dump/restore 分别有证据，不能混称。真实断电/存储损坏未模拟。
- Safari/Firefox、NVDA/VoiceOver、完整键盘/实际 200% 缩放、覆盖率百分比、真实教师专业复核与真实 Word 应用未验证。已确认 UI 与有效历史证据保留。
- 真实模型质量、实时价格、真实数据、远端 CI 和公开服务未验证且未授权。模型适配器测试使用合成 fetcher，不是供应商联调。
- PDF 导出分块约 971 kB、gzip 346 kB，初始 JS gzip 67.51 kB；混合静态/动态导入警告仍存在，没有抬高阈值掩盖。
- 独立清单需与所有仍可恢复的备份一起保留；丢失时拒绝开放。预期指纹必须从独立可信维护记录核对，不能从旧备份自证最新。

这些限制不冒充通过；本次归档是用户已批准的本地成果，不批准生产、真实数据或下一阶段。

## 交付与归档门禁

[测试映射](./TESTS.md)、[安全发现](./SECURITY-REVIEW.md)、[学习卡](./LEARNING-CARD-12.md)、[恢复/升级手册](./RECOVERY-RUNBOOK.md)、[许可清单](./DEPENDENCY-LICENSES.md)、[精确路径清单](./STAGE-12-PATHS.md)。

用户旧 `output/`、预览数据库、忽略的测试日志、dump、清单和截图不在归档范围。最终批准仅允许清单中路径的本地提交；不沿用阶段 11 的归档批准，不自动 tag/push/部署。
