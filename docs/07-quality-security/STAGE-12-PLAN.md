# 阶段 12 质量与安全审查计划

日期：2026-09-05。状态：本地候选审查与获批修复完成，用户已批准 62 路径本地归档，随本次提交记录。

## 基线与授权

用户明确授权进入阶段 12；输入为已批准的阶段 11 本地提交 `97321fe`（96 路径）。开始时仅既有 `output/` 未跟踪；不清理、不暂存。预览数据库与测试数据库分离，保留用户已确认的 Demo、影片和本地任务。

本阶段冻结功能，检查批准的产品契约在代码中的落实，并修复有证据的阻断问题。C2/C3 变化仍先走变更控制。不得读取本机密钥、调用真实模型、使用真实/敏感数据、推送、部署或进入阶段 13。

## 执行顺序与交付

1. `TESTS.md`：MUST/NFR、正常/拒绝结果、实际测试与 CI 对照；区分已运行、计划和未验证。
2. 安全报告：入口、workspace/task 授权、数据库角色、输入输出、会话、缓存日志、幂等、AI/费用、供应链及恢复。每项发现记录精确证据、影响、修复和复验。
3. 运行格式、lint、类型、单元、集成、E2E、依赖审计及生产构建；补查相关浏览器和恢复场景。保留有效旧证据，明确本次执行与继承证据。
4. 提交质量报告、安全报告、依赖许可清单、验证记录、学习卡、遗留风险及精确归档路径。等待新的“批准归档”才可本地提交。

成功条件：批准范围内的阻断问题清零，标准门禁有可复验证据，未验证的设备、专业教师及外部环境不冒充通过。未解决 Critical/High 阻断发布。
