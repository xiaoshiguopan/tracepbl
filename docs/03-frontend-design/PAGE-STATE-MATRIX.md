# 页面状态矩阵

> 现役覆盖说明：深度优化没有降低失败状态门禁；自由回看、本机任务、局部复核与真实导出状态以 `FRONTEND-DEEP-OPTIMIZATION-SPEC.md` 为准。

> 状态：阶段 5 已批准归档
> 当前修订：2026-09-02 P09 一次签发与浏览器原生打印状态已实现
> 规则：每格都是前端必须实现和验证的可观察行为；“无权限”只描述安全展示，真正授权由服务端强制

## 状态矩阵 A：起点、情境、问题与候选

| 页面 | 正常 | 加载 | 空状态 | 校验失败 | 系统失败 | 无权限 | 超时/离线 | 成功反馈 | 手机窄屏 | 键盘和焦点 |
|---|---|---|---|---|---|---|---|---|---|---|
| P00 演示起点 | 强节奏预渲染 CG 影片、王圆箓提灯、自然换行片名、常驻演示/艺术边界和单一主动作从首帧可用；全程无声 | 先显示静态海报；影片按能力加载且不阻塞文字与操作 | 无本机任务时主动作“进入史料工作台”；不制造空白英雄区 | 不设二次边界确认；主动作一次点击即进入 | 媒体解码失败按影片→静态→纯色降级；存储不可用说明临时会话 | 不显示其他任务；异常定位进入 P12 | 离线使用已缓存静态/夹具；影片不可用不反复请求、不假装已播放 | 路由建立后才算进入；匹配剪辑不算业务成功 | 使用独立竖版构图/静态图，无陀螺仪；标题/边界独立可读 | 首焦点为跳至主内容；媒体不进读屏顺序、不抢焦点；reduced-motion 静态终帧并快速切页 |
| P01 教学情境 | 单段最小表单 + 即时条件摘要 + 单一确认动作 | 恢复草稿用稳定骨架；字段不跳动 | 新任务显示合成示例但不冒充用户事实；初步方向可为空 | 顶部摘要 + 可见字段就地错误；聚焦首错，不校验后续步骤隐藏字段 | 显示追踪 ID、保留输入、重试 | P12，不回显任何情境 | 离线可编辑/本机保存；未同步状态明确 | live region 宣告“情境已确认”并给下一步；P02 未实现时诚实说明边界 | 表单单列；当前情境折叠置后；动作不遮焦点 | fieldset/legend 分组；提交聚焦首错；无页签绕行；Tab 顺序自然 |
| P02 探究问题 | 输入、类型判断、建议与证据产物并列 | 建议卡骨架；原输入可读可复制 | 无建议时提供手工编辑路径 | 错误贴近输入；解释范围/课时冲突 | 保留当前确认版；失败建议另区显示 | P12，不泄露问题 | 30秒显示耗时/取消；离线用手工路径 | “中心问题已确认；下一步选择史料” | 建议卡纵向；差异不做并排强制 | 单选/编辑可键盘；生成完成不抢焦点，live region 通知 |
| P03 查找史料 | 推荐 4—6 条 + 更多可用结果；卡内直接阅读史料、来源、推荐理由、可帮助判断/不能单独证明和限制 | 已完成史料继续可读；知识库/外部分项进度不伪造百分比；核验中不进入可用区 | 区分知识库无覆盖、外部无可用结果和筛选后为空；提供调整问题/条件或录入元数据 | 自有材料元数据错误就地显示与摘要，不影响公开候选 | 分项失败，不清空成功结果；不可用项与可选区隔离 | P12；不显示候选或问题是否存在 | 显示已完成范围、单项重试/取消；离线不把未完成核验标为可用 | 选入/排除用 toast + 行内状态；史料组和覆盖缺口同步更新 | 单列史料卡；正文、理由、限制和主动作顺序阅读；无横向表格 | 结果为语义列表；展开按钮有 expanded；选择后焦点不跳；异步更新 polite |

## 状态矩阵 B：证据、活动与量规

| 页面 | 正常 | 加载 | 空状态 | 校验失败 | 系统失败 | 无权限 | 超时/离线 | 成功反馈 | 手机窄屏 | 键盘和焦点 |
|---|---|---|---|---|---|---|---|---|---|---|
| P04 自动核验能力（无独立页面） | 出处/许可、原文/定位、加工、价值/局限结果嵌入 P03 | 分项核验不遮盖其他已知信息；未完成项不进入可用区 | 缺关键字段时在 P03 的未采用结果说明原因 | 元数据补录错误与字段关联 | 冲突/链接失败移出可用区，不假绿 | P12，不暴露来源内容 | 未完成与手工补录路径明确；不假绿 | 权威状态变化在 P03 行内显示并由 live region 宣告 | 完整依据在卡内折叠，状态和限制常显 | 折叠用按钮；来源链接是真链接；展开/返回不丢焦点 |
| P05 主张—证据 | 图形概览 + 完整等价列表；关系可编辑 | 建议加载不锁手工编辑 | 零主张/零关系分别给明确动作 | 关系理由/重复错误就地显示 | 自动分析失败时保留手工列表 | P12 | 离线可编辑本机关系；未完成建议标记 | “关系已保存”；删除可撤销 | 默认列表，图形为可选概览；不要求拖拽 | 所有关系经按钮/对话框操作；图形节点可聚焦并有列表等价 |
| P06 活动与支架 | 连续课堂排演稿、0—45 分钟尺、动作—史料—成果链和时间账簿 | 局部生成有占位；现有草稿可编辑 | 无活动时从已确认关系新建 | 分钟/绑定/产物错误逐段显示 | 生成失败不覆盖手工草稿 | P12 | 30秒取消；离线手工编辑与本机保存 | 保存/重排/撤销有明确反馈 | 排演段落单列；时长摘要置前且不遮焦点 | 排序有“上移/下移”；不依赖拖拽；首错聚焦 |
| P07 评价量规 | 连续评阅标尺、四维三阶段行为锚点、课堂成果和对齐校样 | 结构骨架；不伪造评价内容 | 无量规可从当前成果恢复；缺活动直接返回 P06 | 空泛/重叠/全删证据维度就地显示并聚焦首错 | 保留当前量规和教师编辑，允许继续手工调整 | P12 统一不可用页；前端导航不冒充授权 | 30秒说明；离线仍可编辑和保存本机草稿 | “评价量规已确认”；本机保存失败不假成功 | 校样置前；每维三阶段纵向回流，不隐藏边界 | 首焦点为跳转链接；逐维编辑、替代、移除与撤销均可键盘操作；首错聚焦 |

## 状态矩阵 C：审计、批准、导出与数据

| 页面 | 正常 | 加载 | 空状态 | 校验失败 | 系统失败 | 无权限 | 超时/离线 | 成功反馈 | 手机窄屏 | 键盘和焦点 |
|---|---|---|---|---|---|---|---|---|---|---|
| P08 设计检查 | 预生成结果；异常优先长卷 + 检查校样；通过项收起 | 稳定骨架，不隐藏输入摘要 | 无结果时说明范围并允许形成确定性演示结果；缺上游返回 P07 | 教师确认理由少于 10 或超过 500 字时就地错误并聚焦 | “设计检查未完成”，保留已知内容，未完成项计未知 | P12；前端状态不代表授权 | 30 秒说明耗时；离线外部项计未知且不能完成 | 采用建议自动形成可编辑理由；完成后宣告可进入最终确认与导出 | 检查校样置前，长卷单列；无横向表格 | 首焦点跳至主内容；错误聚焦字段；结果反馈 polite；阻断无忽略入口 |
| P09 最终确认与导出 | 连续装订清样复用上游决定；一次教师签发后在同页打开打印预览 | 读取汇总时显示稳定骨架，不闪回或虚构已知内容 | 缺史料或设计检查未完成时显示精确上游入口 | 文件名清理非法字符；只剩分隔符时就地报错并聚焦 | 本机保存/读取失败保留页面内容；打印只报告窗口已打开，不谎报文件成功 | P12；前端按钮不可视不代表授权 | Demo 离线仍可本机确认/打印；状态未知时不签发 | 确认行内反馈；打开打印窗口后说明由浏览器完成保存 | 交付校样置前、章节单列；问题降为正文重点字号 | 首焦点为跳转链接；单次签发不预勾选；错误聚焦文件名；反馈使用 live region |

P10 不再是独立页面；其导出状态已并入 P09 行，不得重复实现路由或第二套确认状态。
| P11 本机数据与清除 | 用户明确取消，不实现页面 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 不得显示或宣称清除成功 | 不适用 | 不适用 |
| P12 任务不可用 | 统一说明“此任务不可使用” | 不需要对象加载；仅加载公共说明 | 视为安全终态，给新建入口 | 不适用对象表单；参数异常也显示统一页 | 公共说明加载失败仍显示最小静态安全页 | 同一页面，不区分不存在/他人任务 | 离线可新建本机演示任务 | 新任务建立后提示成功 | 单列、大点击目标 | 页面标题获得程序焦点；唯一主动作可键盘触发 |

## 通用验收断言

- 每页标题、主状态和主动作在 320 CSS px 及 200% 文本缩放下可达；来源状态、许可、批准和阻断不因断点消失。
- 每次用户触发异步更新都有文本反馈；自动更新不抢焦点，必要时用 `aria-live="polite"`。
- 加载骨架只表示结构，不伪造内容；系统失败写清“发生什么、保留什么、下一步”。
- 所有页面均有首屏可见的演示边界入口和任务状态入口，但不重复冗长说明。
