# 史证工坊安全基线

> 文档版本：0.1
> 更新日期：2026-08-31
> 状态：已批准归档
> 基准：参考 OWASP ASVS 5.0 的风险导向要求；不宣称认证或完整符合

## 1. 保护对象、参与者与入口

保护对象包括：来源与许可元数据、教师工作内容、任务 capability、模型/数据库/存储密钥、费用额度、审计记录、公开仓库与发布链。

| 参与者 | 权限边界 |
|---|---|
| Demo 访客 | 只操作自己浏览器中的公开/合成数据；无服务端秘密和持久身份 |
| 当前匿名教师 | 凭高熵、短期 capability 访问自己临时任务；不能枚举或访问其他任务 |
| API/Worker 服务身份 | 按职责访问数据库、模型和存储；彼此不共享万能凭据 |
| 仓库维护者 | 运维身份，不是产品角色；其操作通过平台权限和审计管理 |

入口包括公开页面、API 路由、上传/粘贴内容、URL/来源输入、异步任务队列、模型输出、数据库、对象存储、CI/CD 和依赖供应链。v0.1 不含账户、管理后台、邮件、支付、入站 webhook 或定时 cron。

## 2. 信任边界与强制检查

```text
浏览器（不可信） -> API（认证/授权/校验/限额） -> PostgreSQL/对象存储
API -> Worker（签名任务/幂等） -> 模型或检索服务（外部不可信）
模型/检索结果 -> 契约校验 -> 领域门禁 -> 教师明确批准 -> 导出
GitHub 贡献/依赖（不可信） -> CI 最小权限验证 -> Pages 发布
```

| 流程 | 主要威胁 | 必须控制 |
|---|---|---|
| capability 访问任务 | 猜测、泄露、跨任务越权 | 至少128位随机性、传输加密、服务端逐资源授权、过期/撤销、日志脱敏 |
| 输入史料/URL | XSS、SSRF、恶意文件、版权误用 | 类型/大小校验、内容净化、URL允许策略、私网/重定向/DNS复核、许可状态 |
| AI/RAG/Agent | 提示注入、越权工具、伪造引用、费用滥用 | 数据/指令分离、工具允许清单、最小权限、结构校验、引用门禁、预算/超时/停止 |
| 异步任务 | 重放、重复写、无限重试 | 签名或可信队列、幂等键、有限重试、死信/终态、取消 |
| 导出/删除 | IDOR、残留、误操作 | 重新授权、明确确认、级联清理、审计；过期后不恢复 |
| CI/发布 | 恶意 action、secret 泄露、供应链替换 | action 固定完整 SHA、最小 token 权限、依赖锁定、秘密扫描、受控环境 |

## 3. 身份与资源授权

- 无账户不等于无授权。完整模式为每个任务发放短期 capability；任务 ID、URL 可见部分和客户端状态均不能单独授权。
- 每次读、写、导出、取消和删除都在服务端校验 capability、对象归属、状态与动作权限；默认拒绝。
- 无权访问与不存在使用不泄露资源存在性的响应。高影响动作不能只依赖前端隐藏按钮。
- 数据库层是否采用 RLS 在阶段7决定；无论结果如何，API 用例层授权不可省略，并用跨任务负向测试证明。
- capability 最后活动后最长24小时过期；不得记录明文，服务端仅保存不可逆摘要或等价安全表示。

## 4. 输入、输出与浏览器安全

- 所有外部输入有长度、类型、枚举、格式和业务约束；数据库使用参数化查询，命令/模板不得拼接不可信文本。
- 用户或模型 HTML 默认当纯文本；必须渲染富文本时使用允许清单净化并部署严格 CSP。不得使用未净化 `dangerouslySetInnerHTML`。
- 改变状态的浏览器请求采用明确 Origin/CORS 允许清单和 CSRF 防护；不使用 `*` 搭配凭据。
- 服务端取 URL 默认禁止；确需来源抓取时仅允许 HTTP(S)，限制端口、重定向、解析后 IP 和响应大小，阻止 localhost、私网、链路本地及云元数据地址，并在每次重定向后复核。
- 上传采用允许的 MIME/扩展名组合、大小/数量限制、随机存储名、隔离存储和内容检查；不在同源可执行目录直接公开。
- 跳转目标使用内部路由或允许清单；错误和下载文件名做上下文编码。

## 5. 秘密、日志、缓存与第三方

- 密钥只来自本地未跟踪环境或平台 secrets；绝不进入 Git、前端包、fixture、截图、错误响应或测试输出。
- `DATABASE_URL`、模型 Key、对象存储凭据分别最小授权并可独立轮换；泄露即撤销/轮换、查日志和历史、记录影响。
- 日志只保留诊断所需元数据，不记录 capability、Cookie、Authorization、完整史料/提示词或个人数据。审计记录与调试日志分离。
- 敏感响应设不缓存；共享缓存键必须包含安全作用域，清理任务同时清除派生缓存和向量。
- 传给模型提供商的内容先最小化；实际数据保留、训练和跨境条款在启用前复核并取得用户批准。

## 6. 滥用、供应链与恢复

- API 按 capability、IP/风险信号和任务设置速率、并发、大小、Token、次数和金额上限；超限安全失败并保留已完成工作。
- 外部调用有连接/总超时、有限指数退避、熔断和人工重试；不得因失败无限生成任务。
- 锁定依赖，审查许可和维护状态；CI 第三方 action 固定完整提交 SHA、默认只读权限，发布权限与普通测试分离。
- 备份加密、限制访问并做恢复演练；临时任务到期删除优先于备份恢复。生产目标在上线审批前于数据治理中再次确认。
- 事故先隔离入口、撤销密钥、停止相关 Worker/发布，再保存不含敏感内容的证据、评估范围、修复和复盘。

## 7. AI/Agent 特有基线

- 系统指令、工具定义与检索内容分层；网页、文档、OCR 和用户文本永远不能提升权限或改写硬规则。
- Agent 只见完成当前步骤所需的最小数据和工具；默认只读，写操作逐项由应用层授权和校验。
- 模型不能自行批准史料、宣称许可成立、放行审计、删除数据或提高预算。
- 输出必须满足版本化结构；引用逐条验证来源 ID 与定位。验证失败进入“待复核/阻断”，不得猜测补齐。
- 保存模型、提示模板、工具调用、关键输入摘要、费用和结果状态的审计信息；不保存禁止数据。

## 8. 安全验证和例外

发布门禁至少包含：权限负向测试、输入/输出与上传测试、SSRF/重定向测试、秘密扫描、依赖审计、CSP/CORS/CSRF 检查、速率/费用/取消测试、删除与跨任务隔离测试、Agent 越权/提示注入测试、备份恢复演练。

任何未满足项必须在变更记录中写明资产、威胁、补偿控制、风险所有者、到期日和批准者；阻断级问题不得以例外发布。

## 9. 来源

| 来源 | 访问日期 | 适用性 | 不能照搬 |
|---|---|---|---|
| [OWASP ASVS 5.0](https://owasp.org/www-project-application-security-verification-standard/) | 2026-08-31 | Web 技术安全控制与测试要求基准 | 本项目尚未逐条映射或认证；只采用风险相关子集 |
| [OWASP SSRF Prevention Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html) | 2026-08-31 | 未来来源 URL 抓取的应用层和网络层防护 | 具体云网络控制需待部署环境确定 |
| [OWASP GenAI：Excessive Agency](https://genai.owasp.org/llmrisk/llm062025-excessive-agency/) | 2026-08-31 | Agent 工具最小权限、限制自主副作用 | 风险清单不能替代本项目领域门禁 |
| [GitHub Actions 安全使用](https://docs.github.com/en/actions/reference/security/secure-use) | 2026-08-31 | 固定 action SHA、最小 token 与秘密保护 | 平台建议不能证明工作流本身安全 |
