{
  "name": "@tracepbl/database",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "migrate": "node --experimental-strip-types scripts/migrate.ts",
    "seed": "node --experimental-strip-types scripts/seed.ts",
    "test": "vitest run tests/schema-contract.test.ts",
    "test:integration": "vitest run tests/database.integration.test.ts",
    "typecheck": "tsc -b --pretty false"
  },
  "dependencies": {
    "drizzle-orm": "0.45.2",
    "postgres": "3.4.9"
  },
  "devDependencies": {
    "testcontainers": "12.1.0",
    "vitest": "4.1.11"
  }
}
