# Codex 项目引导规则

## 启动顺序

每次工作前依次完整阅读：

1. `VIBE-CODING-AI-GUIDE.md`；
2. `PROJECT-HANDBOOK.md`（生成后）；
3. `docs/00-governance/PROJECT-STATE.md`（生成后）；
4. 当前阶段相关文档；
5. `git status` 与相关差异。

如规则冲突，以用户当前明确指令为最高，其次是项目专属 `PROJECT-HANDBOOK.md`，再其次是总手册。

## 硬门禁

- 技术栈不得预设。立项和优秀案例研究完成后，给出一套唯一结论和证据。
- 规划规范获批前不得进行前端、数据库或后端设计。
- 顺序必须是：前端设计获批 → 前端编码全部完成 → 数据库设计获批 → 数据库编码全部完成 → 后端设计获批 → 后端编码全部完成 → 集成与总审查。
- 规划获批前不得写业务代码；某一层设计获批只授权该层编码，不授权后续层设计或编码。
- 每个子阶段结束必须先报告，等待用户明确回复“批准归档”。此前不得执行 `git add`、`git commit`、`git tag` 或 `git push`。
- “批准归档”只授权本地归档；推送、部署、生产迁移、外发消息、购买资源均需分别授权。
- 文档只进 `docs/`（根目录治理文件除外），代码只进已批准的代码目录。
- 质量与安全验收完成前只使用合成数据；真实数据不得进入 Git、日志、测试、截图或公开仓库。
- 最终交付必须包含 GitHub Public 仓库和无需开发环境即可打开的公开体验 URL；公开动作仍需展示精确目标并取得执行授权。

## 工作方式

- AI 代办优先：能通过当前文件和终端工具安全完成的已授权本地操作，直接执行，不要求零基础用户手动建目录、复制文件或运行 Git/构建命令。
- 只有权限、登录、验证码、Codex 界面选择或实质产品决定需要用户介入；一次只请求一个最小人工动作，完成后立即继续代办。
- 一次只推进一个已批准子阶段；不得顺手实现下一阶段。
- 面向零基础用户解释术语、取舍、验证方式和常见坑。
- 区分已验证事实、推断和未知；不以“应该可以”代替证据。
- 不暴露、记录或提交密码、Token、Cookie、私钥和真实个人数据。
- 不通过删除测试、弱化权限或隐藏错误来制造“通过”。
- 任何改变范围、权限、数据生命周期、费用或外部副作用的歧义都先询问用户。

## 项目规范生成后

立项完成后，按总手册生成 `PROJECT-HANDBOOK.md`，将本文件精简为入口或同步其中稳定规则。不得让两个文件长期互相矛盾。
