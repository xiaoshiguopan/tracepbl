# 史证工坊 Codex 入口规则

具体解释和项目事实以 `PROJECT-HANDBOOK.md` 为准；本文件只保留快速入口和不可越过的硬门禁。

## 每次开工

依次完整阅读：

1. `VIBE-CODING-AI-GUIDE.md`；
2. `PROJECT-HANDBOOK.md`；
3. `docs/00-governance/PROJECT-STATE.md`；
4. 当前阶段文档及相关代码、测试、迁移和配置；
5. `git status`、相关 diff 和最近提交。

随后输出开工摘要。规则冲突时：用户当前明确指令 > `PROJECT-HANDBOOK.md` > 总手册 > 当前阶段文档 > 本文件。

## 项目固定事实

- 产品：史证工坊（TracePBL），面向中学历史教师的可追溯证据探究工作台。
- 当前技术：TypeScript 模块化单体；React/Vite；Hono；PostgreSQL/pgvector；LangGraph.js；可替换的 GLM provider adapter。
- 公开体验：GitHub Pages 静态 Demo，使用公开/合成 fixtures 和预生成结果；不连接秘密后端。
- 完整工程：真实 API、数据库、worker、RAG/Agent 在公开仓库、本地和 CI 中运行与验证。
- 当前流程事实只读 `PROJECT-STATE.md`，不得从本文件猜测阶段。

## 硬门禁

- 阶段 4 总体规划获批前，不进行前端、数据库或后端设计，不安装依赖，不生成脚手架，不写业务代码。
- 固定顺序：前端设计获批 → 前端编码全部完成 → 数据库设计获批 → 数据库编码全部完成 → 后端设计获批 → 后端编码全部完成 → 集成与总审查。
- 一次只推进状态文件中的一个子阶段；某层批准不授权下一层。
- 子阶段先报告和学习卡；用户回复“批准归档”前禁止 `git add`、`git commit`、`git tag`、`git push`。
- “批准归档”只允许报告所列路径的本地提交；push、远程仓库、部署、域名、备案、购买、生产、真实数据和外发消息均需单独授权。
- 文档只进入 `docs/` 或根目录治理文件；代码只进入手册批准的 `apps/`、`packages/`、`database/`、`fixtures/`、`tests/`、`scripts/` 边界。
- 质量与安全验收前只用公开许可或合成数据；真实/个人/学生/敏感数据、秘密、数据库导出和无权材料不得进入 Git、日志、测试、截图或 Demo。
- UI 不直连数据库、对象存储或付费 AI；客户端不得保存密钥；task ID 不是授权。
- 来源未知、外部失败或 AI 推断不得标记为已核验；伪造/错配引用和跨任务访问是发布阻断。
- Agent 不决定权限、删除、引用门禁、教师批准或外部高风险动作。

## 工作与验证

- AI 代办已授权的本地机械操作；仅在登录、验证码、权限或实质产品决定时请求一个最小人工动作。
- 保护用户已有改动；禁止顺手重构、全仓格式化、清理或扩大范围。
- 区分已验证事实、合理推断和未知；只用当前命令输出或可重复步骤声称通过。
- 测试必须覆盖相关失败、越权、边界、重复和恢复，不得删测试、弱化权限或隐藏错误制造通过。
- 范围、权限、数据生命周期、费用、公共接口、依赖或外部副作用变化时，停止并走手册中的变更控制。
